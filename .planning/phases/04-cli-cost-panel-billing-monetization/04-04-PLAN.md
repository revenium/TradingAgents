---
phase: 04-cli-cost-panel-billing-monetization
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified: []
autonomous: false
requirements: [CLI-01, CLI-02, BIL-01, BIL-02]
must_haves:
  truths:
    - "scripts/validate_billing.py succeeds against the live account, confirming the working profitstream host and that a job + outcome appear in Revenium"
    - "A full live ticker run shows the live cost panel updating per agent with the hotspot highlighted and debate agents annotated ×N (CLI-01/CLI-02)"
    - "The completed run produces exactly one billing event and the Revenium Costs & Revenue dashboard shows revenue with positive margin (price > AI cost) (BIL-01/BIL-02/D-08)"
  artifacts: []
  key_links:
    - from: "operator"
      to: "Revenium Costs & Revenue dashboard"
      via: "live run + dashboard inspection"
      pattern: "margin"
---

<objective>
Live end-to-end verification of the monetize pillar and the in-app cost panel — the FCAT demo payoff. This plan adds no code; it confirms the slices from 04-01 (live cost panel) and 04-03 (graph billing emit) work against the real Revenium account, and it resolves the host/key open questions operationally before the demo.

Two gates: (1) host de-risk — run `scripts/validate_billing.py` (from 04-02) live, confirm the working profitstream host and that a job/outcome lands; (2) full-arc verify — run a real ticker, watch the live cost panel, and confirm one billing event with visible margin in the Costs & Revenue dashboard.

Purpose: Prove CLI-01/02 and BIL-01/02 on the live account; lock the host/key config for the demo.
Output: Operator confirmation (no file changes); any required env config (`REVENIUM_BILLING_API_KEY`, `REVENIUM_PROFITSTREAM_BASE_URL`) recorded in `.env`.

Confirms decisions: D-02/D-03/D-05/D-06 visible in-app; D-08 margin visible in dashboard; D-09/D-10 (one event on success, none on halt) observed live.
</objective>

<phase_goal>
ROADMAP **Goal** is in outcome form, not user-story form (`/gsd mvp-phase 04` would formalize it). Derived user story for this phase:

**As a** Revenium demo operator presenting to FCAT, **I want to** run one live ticker and see per-agent cost in the CLI plus revenue-and-margin in the Costs & Revenue dashboard, **so that** the meter → trace → control → monetize arc lands end to end on stage.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-cost-panel-billing-monetization/04-CONTEXT.md
@.planning/phases/04-cli-cost-panel-billing-monetization/04-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Host de-risk — live validate_billing.py</name>
  <action>Operator-driven verification, no code change: run scripts/validate_billing.py against the live account, resolve the working profitstream host, and record it in .env. Follow the steps in how-to-verify.</action>
  <what-built>scripts/validate_billing.py (04-02) calls create_job + report_outcome against the live Revenium account using REVENIUM_BILLING_API_KEY and REVENIUM_PROFITSTREAM_BASE_URL.</what-built>
  <read_first>
    - scripts/validate_billing.py (from 04-02)
    - 04-RESEARCH.md §"Profitstream Host Resolution", Open Question 1, Assumptions A1/A2/A6 — the two candidate hosts (https://api.revenium.io default; https://api.prod.ai.hcapp.io/profitstream/v2/api fallback) and the rev_sk_* key requirement
    - .env.example — where the billing key/host knobs are documented
  </read_first>
  <how-to-verify>
    1. Set in `.env`: `REVENIUM_BILLING_API_KEY` to a `rev_sk_*` (write-scope) key. Leave `REVENIUM_PROFITSTREAM_BASE_URL` unset first (defaults to https://api.revenium.io).
    2. Run: `.venv/bin/python scripts/validate_billing.py --ticker NVDA`.
    3. If checks FAIL or no job appears in Revenium, set `REVENIUM_PROFITSTREAM_BASE_URL=https://api.prod.ai.hcapp.io/profitstream/v2/api` in `.env` and re-run.
    4. Confirm the script prints all checks PASS, the working host, and a job/trace id; then confirm that job + outcome appear in the Revenium jobs/outcomes list.
    5. Record the working host value in `.env` for the demo.
  </how-to-verify>
  <verify>
    <human-check>validate_billing.py exits 0 with all checks PASS; the job + $-valued outcome are visible in Revenium; the working host is recorded in .env.</human-check>
  </verify>
  <done>
    - validate_billing.py exits 0 with all checks PASS against the live account.
    - The created job and its $-valued outcome are visible in Revenium.
    - The working `REVENIUM_PROFITSTREAM_BASE_URL` (and the rev_sk_* key requirement) is recorded in `.env` — Open Question 1 / Assumptions A1/A6 resolved.
  </done>
  <resume-signal>Type "approved" with the confirmed host, or describe the failure (HTTP code / which host).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full-arc live verify — cost panel + billing margin</name>
  <action>Operator-driven verification, no code change: run a full live ticker analysis, observe the live cost panel, and confirm one billing event with positive margin in Revenium's Costs & Revenue dashboard. Follow the steps in how-to-verify.</action>
  <what-built>The live CLI cost panel (04-01) and the graph billing emit (04-03) running together on a real ticker.</what-built>
  <read_first>
    - cli/main.py — `_build_cost_panel` and the `costs` layout column (04-01)
    - tradingagents/graph/trading_graph.py — the _run_graph billing hooks (04-03)
    - 04-RESEARCH.md §"Margin on Dashboard" and Assumption A3 (near-real-time dashboard) — the BIL-02 verification surface and its risk
    - ROADMAP.md §"Phase 4" pricing example ($2.00 = $1.20 cost + $0.80 margin)
  </read_first>
  <how-to-verify>
    1. With billing configured (Task 1) and metering keys present, run a full ticker analysis via the CLI (e.g. `.venv/bin/python -m cli.main analyze` selecting NVDA), watching the terminal.
    2. Confirm the "AI Costs" panel updates per agent as the run proceeds, shows $ primary / tokens secondary, highlights the priciest agent, and renders debate-loop agents (bull/bear researchers, the three risk debators) as one `×N` row with summed cost (CLI-01/CLI-02).
    3. After the run delivers a Portfolio Manager decision, open Revenium's Costs & Revenue dashboard and confirm: one billing event for this run's trace_id, revenue = the configured signal price, and positive margin (price > AI cost) (BIL-01/BIL-02/D-08).
    4. (Optional, D-10 spot check) If feasible, trigger a circuit-breaker halt and confirm NO billing event is emitted for that halted run.
    5. If the dashboard does not show margin in near-real-time (Assumption A3 wrong), note it — a shortened product billing period may be needed; flag for plan/demo risk.
  </how-to-verify>
  <verify>
    <human-check>Live cost panel updates per agent with hotspot + ×N; exactly one billing event with positive margin appears in the Costs & Revenue dashboard.</human-check>
  </verify>
  <done>
    - Live CLI cost panel updates per agent, highlights the hotspot, and shows `×N` on debate agents only (CLI-01/CLI-02 confirmed live).
    - Exactly one billing event for the run appears, with revenue and positive margin visible in the Costs & Revenue dashboard (BIL-01/BIL-02 confirmed live).
    - D-10 behavior (no billing on a halted run) is confirmed or explicitly deferred with rationale.
    - Any dashboard-latency or billing-period caveat (A3) is recorded for the Phase 5 demo hardening.
  </done>
  <resume-signal>Type "approved" once panel + margin are confirmed, or describe what did not appear.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator screen / stage → audience | Live panel and dashboard are shown on stage; must not expose key material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | live run output + dashboard on stage | mitigate | Cost panel and validate_billing.py output contain no key material (enforced in 04-01/04-02). Operator confirms no secret is rendered before presenting. |
| T-04-09 | Tampering | live billing event correctness | mitigate | Exactly one event per completed run observed live (D-09); halted-run no-bill spot-checked (D-10). |
| T-04-SC | Tampering | pip installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Host/key resolved and recorded in `.env` (Open Question 1, A1/A2/A6).
- CLI-01/02 confirmed live; BIL-01/02 confirmed in the Costs & Revenue dashboard.
- Any A3 dashboard-latency caveat captured for Phase 5.
</verification>

<success_criteria>
- The full Phase 4 monetize slice is proven live: live cost panel with ×N hotspot + one billing event with visible margin.
- Demo-blocking host/key questions are resolved and recorded before FCAT.
</success_criteria>

<output>
Create `.planning/phases/04-cli-cost-panel-billing-monetization/04-04-SUMMARY.md` when done.
</output>
