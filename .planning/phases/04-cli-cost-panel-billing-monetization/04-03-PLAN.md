---
phase: 04-cli-cost-panel-billing-monetization
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - tradingagents/graph/trading_graph.py
  - tests/test_billing_graph_hook.py
autonomous: true
requirements: [BIL-01, BIL-02]
must_haves:
  truths:
    - "A completed run (graph delivers a final PM decision) emits exactly one billing event with outcomeValue = signal price (D-09)"
    - "A circuit-breaker-halted run (BudgetExceededError) emits NO billing event (D-10)"
    - "The job is created at run start (after begin_run) so revenue correlates to the run's trace_id, letting Revenium pair revenue with AI cost as margin (D-08/BIL-02)"
    - "end_run / stop_polling still always fire in finally regardless of billing outcome (fail-open posture preserved)"
  artifacts:
    - path: "tradingagents/graph/trading_graph.py"
      provides: "_billing_emitter instantiated in __init__; create_trading_signal_job + emit_billing_event hooks in _run_graph"
      contains: "TradingSignalBillingEmitter"
    - path: "tests/test_billing_graph_hook.py"
      provides: "keyless placement tests: one emit on success, zero emit on BudgetExceededError"
  key_links:
    - from: "tradingagents/graph/trading_graph.py _run_graph"
      to: "self._billing_emitter.emit_billing_event"
      via: "call inside try after final_state, before return (NOT in finally)"
      pattern: "emit_billing_event"
    - from: "tradingagents/graph/trading_graph.py __init__"
      to: "TradingSignalBillingEmitter.from_config"
      via: "instantiation alongside ReveniumCallbackHandler"
      pattern: "TradingSignalBillingEmitter.from_config"
---

<objective>
Wire the billing emitter (built and host-validated in plan 04-02) into the graph run lifecycle (BIL-01, BIL-02). Instantiate `TradingSignalBillingEmitter` in `TradingAgentsGraph.__init__`, create the job at run start, and emit exactly one billing outcome on the success path after the graph delivers a final PM decision. Because `BudgetExceededError` propagates out of `graph.invoke()` before the emit line is reached, circuit-breaker-halted runs are structurally non-billable — no conditional needed (D-10).

This completes the monetize slice end to end: a real completed run produces revenue that Revenium pairs with the run's AI cost as margin in the Costs & Revenue dashboard.

Purpose: Emit the priced "cost per trading signal" event from the one correct place (D-09), never from a halted run (D-10).
Output: Graph hooks + keyless placement tests proving the success/halt billing contract.

Implements decisions: D-08 (margin = price − AI cost, correlated via trace_id), D-09 (exactly one emit on the success path), D-10 (no emit on halt).

Dependency note: depends_on 04-02 — `billing.py` must exist and the Jobs/Outcomes path should be host-validated via `scripts/validate_billing.py` (from 04-02) before this wiring is exercised live. The host itself is config-driven (`revenium_profitstream_url`), so this code is host-agnostic.
</objective>

<phase_goal>
ROADMAP **Goal** is in outcome form, not user-story form (`/gsd mvp-phase 04` would formalize it). Derived user story for this slice:

**As a** Revenium demo operator, **I want to** have each completed trading run automatically emit one priced billing event (and halted runs emit none), **so that** the Costs & Revenue dashboard shows real revenue and margin for the work that actually delivered a signal.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-cost-panel-billing-monetization/04-CONTEXT.md
@.planning/phases/04-cli-cost-panel-billing-monetization/04-RESEARCH.md
@.planning/phases/04-cli-cost-panel-billing-monetization/04-PATTERNS.md
@tradingagents/revenium/billing.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Billing hooks in _run_graph + __init__ instantiation</name>
  <files>tradingagents/graph/trading_graph.py, tests/test_billing_graph_hook.py</files>
  <read_first>
    - tradingagents/graph/trading_graph.py — `_run_graph` try/finally skeleton (~lines 386–479), the `begin_run`/`end_run`/`stop_polling` placement, the debug vs non-debug `final_state` construction, `__init__` handler instantiation (~lines 74–83), the Revenium import block (~lines 36–37)
    - tradingagents/revenium/billing.py (from 04-02) — `TradingSignalBillingEmitter`, `from_config`, `create_trading_signal_job(trace_id, ticker, trade_date)`, `emit_billing_event(trace_id, signal_price, reported_by)`
    - 04-PATTERNS.md §"tradingagents/graph/trading_graph.py" — exact hook placement (after begin_run; after final_state merge, before return; the critical "never in finally" rule)
    - 04-RESEARCH.md Pitfall 1 (report_outcome in finally bills halted runs) and §"Billing API Path" (two-call flow)
    - tests/test_revenium_tracing.py — conventions for exercising _run_graph / handler lifecycle with MagicMock and asserting call args (analog for the placement test)
    - tests/test_revenium_enforcement.py — BudgetExceededError import and how a breached-rule raise is simulated
  </read_first>
  <action>
    In `trading_graph.py`, add `from tradingagents.revenium.billing import TradingSignalBillingEmitter` to the Revenium import block. In `__init__`, after the `ReveniumCallbackHandler` setup, instantiate `self._billing_emitter = TradingSignalBillingEmitter.from_config(self.config)` (mirrors the existing handler instantiation pattern; disabled/no-op when no billing key per 04-02). In `_run_graph`, inside the `try` block immediately after `self._revenium_handler.begin_run(_trace_id, company_name, str(trade_date))`, call `self._billing_emitter.create_trading_signal_job(trace_id=_trace_id, ticker=company_name, trade_date=str(trade_date))`. Then add the success emit: after `final_state` is fully populated in BOTH the debug (chunk-merge) and non-debug (`graph.invoke`) branches, and BEFORE `self.curr_state = final_state` / the `return`, call `self._billing_emitter.emit_billing_event(trace_id=_trace_id, signal_price=self.config.get("revenium_signal_price", 2.00))`. This line MUST be inside the `try` (after the graph call) and MUST NOT be in `finally` (Pitfall 1 / D-10: `BudgetExceededError` raised inside `graph.invoke()` propagates before this line, so halted runs never bill). Leave `end_run()` / `stop_polling()` in `finally` exactly as-is (they must always fire). Do not block the node — `emit_billing_event` is already fire-and-forget on a daemon thread (04-02). Write `tests/test_billing_graph_hook.py` (`@pytest.mark.unit`, keyless): construct a `TradingAgentsGraph` (or the minimal seam used by existing tracing tests) with `self.graph` patched and `self._billing_emitter` replaced by a MagicMock. Test A (success): patch the graph call to return a final_state containing `final_trade_decision`; run `_run_graph`; assert `_billing_emitter.create_trading_signal_job` called once and `_billing_emitter.emit_billing_event` called exactly once with `signal_price == config["revenium_signal_price"]`. Test B (halt): patch the graph call to raise `BudgetExceededError`; run `_run_graph` expecting the error to propagate; assert `create_trading_signal_job` called once, `emit_billing_event` NOT called, and `end_run` still called (finally fired). If constructing the full graph is too heavy, factor the emit into a small private helper and unit-test the placement via a lightweight stub that reproduces the try/finally control flow — but the assertion target remains: one emit on success, zero on BudgetExceededError.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python -m pytest tests/test_billing_graph_hook.py tests/test_revenium_enforcement.py tests/test_revenium_tracing.py -q && .venv/bin/python -m ruff check tradingagents/graph/trading_graph.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_billing_graph_hook.py` passes keyless and asserts: exactly one `emit_billing_event` on the success path; zero on the `BudgetExceededError` path; `end_run` always fires.
    - `grep -n "emit_billing_event" tradingagents/graph/trading_graph.py` shows the call inside the `try` block, NOT in the `finally` block (verify by reading surrounding context that it sits after the graph call and before `return`).
    - `grep -n "TradingSignalBillingEmitter.from_config" tradingagents/graph/trading_graph.py` confirms instantiation in `__init__`.
    - Existing enforcement and tracing tests still pass (no regression to begin_run/end_run/stop_polling lifecycle).
    - `ruff check` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph run lifecycle → Revenium Jobs/Outcomes API | The success-path emit posts a revenue value tied to the run's trace_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Repudiation / Tampering | double-billing or billing a halted run | mitigate | Exactly one emit on the success path (D-09); emit is after `graph.invoke()` so `BudgetExceededError` halts bill nothing (D-10). `create_job` uses the unique per-run trace_id (idempotent re-run safe via SDK 409 handling). |
| T-04-07 | Denial of Service | billing emit delaying graph teardown | mitigate | Emit is fail-open on a daemon thread (04-02); `end_run`/`stop_polling` remain in `finally` and always fire even if the emit thread is slow or the host is unreachable. |
| T-04-SC | Tampering | pip installs | accept | No new packages; emitter from already-vendored SDK. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- Keyless suite green; placement contract proven by unit tests (one emit on success, zero on halt).
- `end_run`/`stop_polling` unchanged in `finally`; fail-open posture preserved.
- Host-agnostic wiring (price from config; host from `revenium_profitstream_url` set in 04-02).
</verification>

<success_criteria>
- A completed run emits exactly one priced billing event correlated to its trace_id (BIL-01).
- Halted runs emit none (D-10).
- Revenue is correlated to AI cost via trace_id so margin can surface in Revenium's Costs & Revenue dashboard (BIL-02, confirmed live in plan 04-04).
</success_criteria>

<output>
Create `.planning/phases/04-cli-cost-panel-billing-monetization/04-03-SUMMARY.md` when done.
</output>
