---
phase: 01-foundation-metering
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - cli/main.py
  - tradingagents/agents/analysts/market_analyst.py
  - tradingagents/agents/analysts/sentiment_analyst.py
  - tradingagents/agents/analysts/news_analyst.py
  - tradingagents/agents/analysts/fundamentals_analyst.py
  - tradingagents/agents/researchers/bull_researcher.py
  - tradingagents/agents/researchers/bear_researcher.py
  - tradingagents/agents/managers/research_manager.py
  - tradingagents/agents/trader/trader.py
  - tradingagents/agents/risk_mgmt/aggressive_debator.py
  - tradingagents/agents/risk_mgmt/conservative_debator.py
  - tradingagents/agents/risk_mgmt/neutral_debator.py
  - tradingagents/agents/managers/portfolio_manager.py
  - tradingagents/agents/utils/core_stock_tools.py
  - tradingagents/agents/utils/technical_indicators_tools.py
  - tradingagents/agents/utils/fundamental_data_tools.py
  - tradingagents/agents/utils/news_data_tools.py
  - tradingagents/agents/utils/macro_data_tools.py
  - tradingagents/agents/utils/prediction_markets_tools.py
  - tradingagents/agents/utils/market_data_validation_tools.py
  - tradingagents/revenium/callback.py
  - tests/test_revenium_tool_metering.py
  - scripts/validate_metering.py
autonomous: false
requirements: [MTR-03, MTR-04]

must_haves:
  truths:
    - "Every agent node sets its own internal node name as the current_agent_name contextvar so events are attributed per-agent"
    - "A multi-provider run shows two distinct provider labels (Anthropic and OpenAI) in Revenium"
    - "Analyst data-fetch tool calls appear in Revenium as separate metered events distinct from LLM token events"
    - "The sentiment_analyst .func bypass is documented as the single explicit @meter_tool exemption"
    - "No LLM-level streaming exists, so on_llm_end receives complete token data (research-flag confirmed)"
    - "The CLI path wires the same single ReveniumCallbackHandler used by the graph (no second handler / no double-counting)"
  artifacts:
    - path: "cli/main.py"
      provides: "Revenium handler added to CLI callbacks + graph args"
      contains: "ReveniumCallbackHandler"
    - path: "tradingagents/agents/utils/core_stock_tools.py"
      provides: "@meter_tool on data-fetch tools (cost iceberg)"
      contains: "meter_tool"
    - path: "tests/test_revenium_tool_metering.py"
      provides: "Mocked tool-event metering + agent-attribution test"
      contains: "def test_"
  key_links:
    - from: "agent node functions"
      to: "current_agent_name contextvar"
      via: "first-line set with the internal node name (D-12)"
      pattern: "current_agent_name.set"
    - from: "cli/main.py"
      to: "ReveniumCallbackHandler"
      via: "from_config + appended to callbacks and get_graph_args"
      pattern: "ReveniumCallbackHandler"
    - from: "data-fetch tools"
      to: "Revenium tool events"
      via: "@meter_tool decorator surfaced through agent_utils"
      pattern: "meter_tool"
---

<objective>
Thicken the proven metering slice to full coverage: set the per-agent identity contextvar at the top of all 12 agent node functions (D-12), wire the same single handler into the CLI path, decorate every analyst data-fetch tool with `@meter_tool` for the "cost iceberg" (D-13), confirm the multi-provider labels (Anthropic vs OpenAI) appear distinctly (MTR-04), and confirm via grep that no LLM-level streaming exists (research-flag) so `on_llm_end` token data is complete.

Purpose: Plan 02 proved ONE call meters correctly. This plan scales that to EVERY agent and EVERY data-fetch tool across both providers, completing the metering pillar so a full `propagate()` run yields per-agent, per-provider, tool-vs-token cost visibility — the complete "cost iceberg" the FCAT demo needs.

Output: 12 one-line contextvar sets in agent nodes, CLI wiring in `cli/main.py`, `@meter_tool` on the data-fetch tools across 7 tool modules, the documented sentiment `.func` exemption, a mocked tool-metering test, and a multi-provider assertion extension to `validate_metering.py`.

Covers: MTR-03 (@meter_tool on data-fetch tools + documented .func exemption), MTR-04 (distinct Anthropic vs OpenAI provider labels).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-metering/01-CONTEXT.md
@.planning/phases/01-foundation-metering/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-metering/01-02-SUMMARY.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-agent identity contextvar in all 12 agent nodes + confirm no LLM-level streaming (D-12)</name>
  <files>tradingagents/agents/analysts/market_analyst.py, tradingagents/agents/analysts/sentiment_analyst.py, tradingagents/agents/analysts/news_analyst.py, tradingagents/agents/analysts/fundamentals_analyst.py, tradingagents/agents/researchers/bull_researcher.py, tradingagents/agents/researchers/bear_researcher.py, tradingagents/agents/managers/research_manager.py, tradingagents/agents/trader/trader.py, tradingagents/agents/risk_mgmt/aggressive_debator.py, tradingagents/agents/risk_mgmt/conservative_debator.py, tradingagents/agents/risk_mgmt/neutral_debator.py, tradingagents/agents/managers/portfolio_manager.py</files>
  <read_first>
    - tradingagents/agents/analysts/market_analyst.py (the create_*(llm) factory / inner *_node(state) pattern — the analog for all 12 edits; insert point is the first line of the node body)
    - tradingagents/revenium/context.py (current_agent_name contextvar from Plan 02)
    - .planning/phases/01-foundation-metering/01-PATTERNS.md section "Agent node one-liners (13 files ...)" — the exact agent-name string table (D-12) and the .func exemption note
    - .planning/phases/01-foundation-metering/01-CONTEXT.md code_context streaming-correction note (graph-level .stream() is expected; only LLM-level llm.stream/astream would require a tiktoken fallback)
    - .planning/research/SUMMARY.md "Research Flags" (Phase 1: grep agents/ and llm_clients/ for stream)
  </read_first>
  <action>
    Add as the FIRST line of each agent's inner *_node(state) body a one-line contextvar set using the internal graph node name (D-12), importing current_agent_name from tradingagents.revenium.context. Use the exact names from the PATTERNS table: market_analyst.py uses "market_analyst"; sentiment_analyst.py uses "sentiment_analyst"; news_analyst.py uses "news_analyst"; fundamentals_analyst.py uses "fundamentals_analyst"; bull_researcher.py uses "bull_researcher"; bear_researcher.py uses "bear_researcher"; research_manager.py uses "research_manager" (apply to each node defined in that file, including any judge/manager node — all "research_manager"); trader.py uses "trader"; aggressive_debator.py uses "aggressive_debator"; conservative_debator.py uses "conservative_debator"; neutral_debator.py uses "neutral_debator"; portfolio_manager.py uses "portfolio_manager". These node-name strings MUST match the revenium_task_type_map keys from Plan 01 so task_type resolves. Do NOT change any graph topology, state shape, or return dicts — this is a one-line additive set per node. As the research-flag verification, grep agents/ and llm_clients/ for LLM-level streaming (llm.stream / astream / stream=True / streaming=True): confirm NONE exists (graph-level self.graph.stream in trading_graph.py:384 and cli/main.py:1166 is expected and harmless). Record the grep result in the SUMMARY. Only if LLM-level streaming were found would a tiktoken fallback be needed in on_llm_end — it is not expected; do not add one.
  </action>
  <verify>
    <automated>grep -rl "current_agent_name.set" tradingagents/agents/ | wc -l</automated>
    <automated>for f in market_analyst sentiment_analyst news_analyst fundamentals_analyst bull_researcher bear_researcher research_manager trader aggressive_debator conservative_debator neutral_debator portfolio_manager; do grep -rq "current_agent_name.set(\"$f\")" tradingagents/agents/ && echo "OK $f" || echo "MISSING $f"; done</automated>
    <automated>test -z "$(grep -rn 'llm\.stream\|llm\.astream\|stream=True\|streaming=True' tradingagents/agents/ tradingagents/llm_clients/)" && echo PASS-no-llm-streaming || echo FOUND-llm-streaming-add-tiktoken-fallback</automated>
  </verify>
  <acceptance_criteria>
    - All 12 agent node files contain current_agent_name.set("<node_name>") with the exact D-12 names matching revenium_task_type_map keys
    - No agent return dict, state field, or graph edge is modified (additive one-liner only)
    - grep for llm.stream/astream/stream=True/streaming=True across agents/ and llm_clients/ returns nothing (research-flag confirmed; SUMMARY records it)
  </acceptance_criteria>
  <done>Each LLM event is attributed to the correct agent (and thus the correct task_type bucket) with zero topology change; the non-streaming assumption is verified.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the single Revenium handler into the CLI path (no second handler)</name>
  <files>cli/main.py</files>
  <read_first>
    - cli/main.py lines 1041-1062 (stats_handler construction + TradingAgentsGraph(callbacks=[stats_handler])) and line 1147-1166 (propagator.get_graph_args(callbacks=[stats_handler]) for tool tracking)
    - .planning/phases/01-foundation-metering/01-PATTERNS.md section "cli/main.py (edit ...)" (the three exact edit points)
    - tradingagents/revenium/callback.py (from_config + enabled, from Plan 02)
    - tradingagents/graph/trading_graph.py (the dedup logic from Plan 02 Task 3 — a caller-supplied handler is not duplicated)
    - .planning/research/PITFALLS.md Pitfall 1 (do NOT create a second metering path; one handler instance)
  </read_first>
  <action>
    Edit cli/main.py per the PATTERNS map. (1) Near where stats_handler is constructed (~line 1041), construct one ReveniumCallbackHandler via from_config(config) — it is a silent no-op when REVENIUM_METERING_API_KEY is absent (D-05). (2) Extend the TradingAgentsGraph(...) callbacks kwarg to [stats_handler] + ([revenium_handler] if revenium_handler.enabled else []). (3) At the tool-tracking call (~line 1162) extend get_graph_args callbacks the same way so analyst tool execution is also tracked by the SAME handler instance. Reuse the SAME revenium_handler object in both places — do NOT construct a second handler (would double-count, Pitfall 1). Because TradingAgentsGraph (Plan 02 Task 3) dedups a caller-supplied enabled ReveniumCallbackHandler against the one it would otherwise build internally, passing the CLI handler into the graph results in exactly one enabled handler active for LLM calls. Add a comment documenting that the CLI-built handler is the single source and the graph dedup prevents a second metering path.
  </action>
  <verify>
    <automated>grep -c "ReveniumCallbackHandler" cli/main.py</automated>
    <automated>grep -c "revenium_handler.enabled" cli/main.py</automated>
    <automated>REVENIUM_METERING_API_KEY= .venv/bin/python -c "import cli.main; print('PASS-cli-imports-keyless')"</automated>
  </verify>
  <acceptance_criteria>
    - cli/main.py constructs exactly one ReveniumCallbackHandler and reuses it in both the TradingAgentsGraph callbacks kwarg and get_graph_args
    - Exactly one enabled ReveniumCallbackHandler instance ends up active per run (no duplicate handler / no second metering path), relying on the graph dedup; documented in a comment
    - cli.main imports cleanly with no Revenium key set
  </acceptance_criteria>
  <done>The CLI run meters via the same single handler the graph uses; tool execution is tracked; no double-counting path is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: @meter_tool on all analyst data-fetch tools + documented .func exemption (MTR-03)</name>
  <files>tradingagents/agents/utils/core_stock_tools.py, tradingagents/agents/utils/technical_indicators_tools.py, tradingagents/agents/utils/fundamental_data_tools.py, tradingagents/agents/utils/news_data_tools.py, tradingagents/agents/utils/macro_data_tools.py, tradingagents/agents/utils/prediction_markets_tools.py, tradingagents/agents/utils/market_data_validation_tools.py, tradingagents/agents/analysts/sentiment_analyst.py</files>
  <read_first>
    - tradingagents/agents/utils/agent_utils.py lines 1-45 (the re-export hub + __all__ list of get_* tools — the public tool surface)
    - tradingagents/agents/utils/core_stock_tools.py, technical_indicators_tools.py, fundamental_data_tools.py, news_data_tools.py, macro_data_tools.py, prediction_markets_tools.py, market_data_validation_tools.py (the 7 modules where @tool data-fetch functions are defined)
    - tradingagents/agents/analysts/sentiment_analyst.py lines 60-75 (the get_news.func(...) direct-call bypass at ~line 69 — the documented exemption)
    - .planning/research/STACK.md (revenium-metering provides @meter_tool / meter_tool decorator)
    - .planning/research/PITFALLS.md Pitfall 2 ("Looks Done But Isn't" .func bypass note) and the MTR-03 acceptance in REQUIREMENTS.md
    - tradingagents/revenium/context.py (meter_tool events should carry trace_id/agent context like LLM events)
  </read_first>
  <behavior>
    - Each decorated data-fetch tool, when invoked through its @tool wrapper, fires exactly one Revenium tool-metering event carrying the tool name and the current trace_id/agent context.
    - The decorator is fail-open: a metering error does not break the data fetch (the tool still returns its data / NO_DATA_AVAILABLE sentinel).
    - With no Revenium key set, the decorator is a transparent pass-through (silent no-op) and the tool behaves identically to undecorated.
  </behavior>
  <action>
    Apply the revenium-metering @meter_tool decorator to every analyst data-fetch tool (D-13): the get_* functions exported via agent_utils.py — get_stock_data, get_indicators, get_fundamentals, get_balance_sheet, get_cashflow, get_income_statement, get_news, get_global_news, get_insider_transactions, get_macro_indicators, get_prediction_markets, get_verified_market_snapshot — defined across the 7 tool modules. The decorator must wrap the data-fetch so the tool event carries the current trace_id and agent (from contextvars) and stays fail-open (a metering failure must not block the fetch; the tool still returns data or the NO_DATA_AVAILABLE sentinel — repo convention). If the SDK's @meter_tool does not natively read our contextvars, wrap it in a thin local adapter in tradingagents/revenium/ (e.g., a meter_tool re-export that injects trace_id/agent and applies fail-open) and import that from the tool modules — keep provider-neutral, no per-tool special casing. Order the decorator correctly relative to the existing @tool decorator so LangChain still sees a valid tool (the @tool wrapper must remain the outermost so the LangChain tool contract is preserved; @meter_tool wraps the underlying function beneath it). DOCUMENT the single exemption: at sentiment_analyst.py ~line 69 where get_news.func(...) is called directly (bypassing the @tool wrapper, so @meter_tool does not fire for that one data-fetch), add an inline comment explaining it is a deliberate documented exemption (D-13) — a data fetch, not an LLM call; do NOT force-fix the bypass.
  </action>
  <verify>
    <automated>grep -rl "meter_tool" tradingagents/agents/utils/ | wc -l</automated>
    <automated>grep -rn "exemption\|meter_tool" tradingagents/agents/analysts/sentiment_analyst.py</automated>
    <automated>REVENIUM_METERING_API_KEY= .venv/bin/python -c "from tradingagents.agents.utils.agent_utils import get_stock_data, get_news, get_fundamentals; print('PASS-tools-import-keyless')"</automated>
    <automated>REVENIUM_METERING_API_KEY= .venv/bin/python -m pytest tests/test_market_toolnode.py tests/test_vendor_routing.py -q; echo "tool-suite exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - All 12 get_* data-fetch tools exported via agent_utils carry @meter_tool (the meter_tool token appears in each of the 7 tool modules that define them)
    - sentiment_analyst.py ~line 69 carries an inline comment documenting the get_news.func bypass as the single explicit exemption (D-13); the bypass is NOT force-fixed
    - With no key set, decorated tools import and behave identically (silent no-op) — existing tool tests still pass
    - A metering failure inside the decorator does not break the data fetch (fail-open)
  </acceptance_criteria>
  <done>Data-fetch tool calls surface as separate metered events (the cost iceberg: tool cost vs token cost), with the one .func bypass documented rather than masked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Mocked tool-metering + agent-attribution test; multi-provider assertion in validate_metering (MTR-04)</name>
  <files>tests/test_revenium_tool_metering.py, scripts/validate_metering.py</files>
  <read_first>
    - tests/test_revenium_metering.py (from Plan 02 — reuse its mock/fixture conventions for the client/handler)
    - tradingagents/revenium/callback.py and the meter_tool adapter from Task 3
    - scripts/validate_metering.py (from Plan 02 — extend, do not rewrite)
    - .planning/research/STACK.md "Provider Coverage Matrix" (ChatAnthropic -> "anthropic", ChatOpenAI -> "openai"; base-URL variants mislabel as openai — for the demo the Anthropic+OpenAI pair is the clean two-label story)
    - .planning/research/PITFALLS.md "Looks Done But Isn't" checklist (tool events distinct from LLM events; provider labels distinct)
  </read_first>
  <behavior>
    - test (tool event): invoking a decorated data-fetch tool inside a revenium_run_context with a mocked client fires exactly one tool-metering event carrying the tool name and the active trace_id; with no key it fires zero.
    - test (agent attribution): driving the handler with current_agent_name set to two different agents produces events whose agent + task_type differ accordingly (e.g., market_analyst->analysis, bull_researcher->research_debate).
    - validate_metering.py (extended): a two-provider probe (one ChatAnthropic call + one ChatOpenAI call) yields two events whose provider labels are distinct ("anthropic" and "openai").
  </behavior>
  <action>
    Create tests/test_revenium_tool_metering.py (marker: unit) with: (a) a tool-event test — patch the metering client, invoke a decorated tool (e.g., get_stock_data through its wrapper) inside revenium_run_context, assert exactly one tool event recorded carrying the tool name and the active trace_id; and a keyless variant asserting zero events and identical return value; (b) an agent-attribution test — set current_agent_name to "market_analyst" then "bull_researcher", drive on_chat_model_start/on_llm_end, assert recorded events carry agent="market_analyst"/taskType="analysis" and agent="bull_researcher"/taskType="research_debate" respectively. Mock all HTTP; no live key; must pass under REVENIUM_METERING_API_KEY=. Extend scripts/validate_metering.py (do not rewrite Plan 02's single-call assertion) to add a --multi-provider mode that makes one Anthropic LLM call and one OpenAI LLM call (via create_llm_client with the two configured providers/models) inside the run context, then asserts the resulting Revenium events show two DISTINCT provider labels ("anthropic" and "openai") — MTR-04. Print PASS/FAIL per provider; return 0/1. Keep the default (no-flag) behavior as the Plan 02 one-event assertion.
  </action>
  <verify>
    <automated>REVENIUM_METERING_API_KEY= .venv/bin/python -m pytest tests/test_revenium_tool_metering.py -q; echo "exit=$?"</automated>
    <automated>REVENIUM_METERING_API_KEY= .venv/bin/python -m pytest tests/ -q -m unit; echo "full-unit-suite exit=$?"</automated>
    <automated>grep -c "multi-provider\|multi_provider\|anthropic\|openai" scripts/validate_metering.py</automated>
  </verify>
  <acceptance_criteria>
    - REVENIUM_METERING_API_KEY= python -m pytest tests/test_revenium_tool_metering.py -q exits 0
    - The tool-event test asserts exactly one tool event with the tool name + active trace_id; keyless variant asserts zero and identical return
    - The agent-attribution test asserts distinct agent + task_type per agent (analysis vs research_debate)
    - validate_metering.py --multi-provider asserts two distinct provider labels (anthropic, openai)
    - The full unit suite still passes with no Revenium key set
  </acceptance_criteria>
  <done>Tool metering and per-agent attribution are guarded in the keyless suite; the live script proves the two-provider cost-breakdown story (MTR-04).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Verify full-graph per-agent + tool + two-provider attribution</name>
  <files>scripts/validate_metering.py</files>
  <action>Pause for the human to run validate_metering.py --multi-provider and a full CLI analysis, then confirm per-agent + tool + two-provider attribution in Revenium with nothing UNCLASSIFIED and no doubled events. No code is written in this task; it verifies Tasks 1-4.</action>
  <what-built>
    Full metering coverage: 12 per-agent contextvar sets, CLI wiring (single handler, no double-count), @meter_tool on all data-fetch tools with the documented .func exemption, mocked tool/attribution tests (passing keyless), and validate_metering.py --multi-provider. Claude has run the keyless suite (green) and confirmed no LLM-level streaming. The full live run + multi-provider label check need your Revenium key + account.
  </what-built>
  <how-to-verify>
    1. With REVENIUM_METERING_API_KEY (rev_mk_*) set, run `.venv/bin/python scripts/validate_metering.py --multi-provider` — expect two events with distinct provider labels "anthropic" and "openai", exit 0.
    2. Run a full analysis via the CLI (`tradingagents analyze`) on a ticker. In the Revenium dashboard confirm: (a) multiple LLM events attributed to distinct agents (market_analyst, bull_researcher, research_manager, portfolio_manager, etc.) with the expected task_type buckets (analysis/research_debate/planning/trade/risk_debate/decision); (b) separate tool-metering events for data fetches (the cost iceberg — tool cost vs token cost is visible); (c) two provider labels (Anthropic and OpenAI) in the cost breakdown; (d) nothing in UNCLASSIFIED.
    3. Confirm the run shows NO doubled LLM events (each LLM call appears once — double-counting sentinel across the full graph).
  </how-to-verify>
  <resume-signal>Type "approved" once a full run shows per-agent + tool + two-provider attribution with nothing UNCLASSIFIED and no doubled events, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data-fetch tool → Revenium API | Tool events cross here; tool inputs (ticker, dates) are low-sensitivity but still must not leak keys |
| CLI handler → graph callbacks | Risk of wiring two handlers and double-counting across the CLI + graph paths |
| agent node → contextvar | Per-agent identity correctness drives attribution accuracy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | duplicate handler across CLI + graph → 2x cost | mitigate | Exactly one ReveniumCallbackHandler reused/deduplicated across CLI callbacks, get_graph_args, and the graph's own wiring; full-run sentinel confirms no doubled LLM events (Pitfall 1) |
| T-03-02 | Information Disclosure | @meter_tool adapter fail-open logging | mitigate | Tool-metering failures log only the tool name (symbolic), never keys or full request bodies (D-06); decorator is fail-open and does not alter tool return |
| T-03-03 | Denial of Service | metering inside the tool path blocking data fetch | mitigate | Decorator is fail-open and must not block or fail the underlying fetch; existing tool tests confirm identical behavior with no key |
| T-03-04 | Repudiation/Integrity | wrong agent label → misattributed cost | mitigate | D-12 node-name strings asserted by grep against revenium_task_type_map keys; agent-attribution unit test pins agent->task_type mapping |
| T-03-SC | Tampering | @meter_tool from revenium-metering (already installed Plan 01) | mitigate | No new package install in this plan; uses the revenium-metering already pinned + verified in Plan 01 (no new supply-chain surface) |
</threat_model>

<verification>
- All 12 agent nodes set the correct D-12 contextvar (grep) and no topology changed.
- No LLM-level streaming exists (research-flag grep) — on_llm_end token data is complete.
- CLI uses exactly one enabled handler; no double-counting path.
- All 12 data-fetch tools carry @meter_tool; the sentiment .func bypass is documented.
- Keyless unit suite (tool + attribution tests) green.
- Live: validate_metering.py --multi-provider shows two distinct provider labels; a full run shows per-agent + tool + two-provider attribution, nothing UNCLASSIFIED, no doubled events.
</verification>

<success_criteria>
- MTR-03: @meter_tool on all analyst data-fetch tools makes tool cost vs token cost visible; the sentiment .func bypass is the single documented exemption.
- MTR-04: a multi-provider run shows correct, distinct Anthropic vs OpenAI provider labels in Revenium's cost breakdown.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-metering/01-03-SUMMARY.md` when done.
</output>
