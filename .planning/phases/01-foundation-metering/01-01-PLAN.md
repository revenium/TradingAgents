---
phase: 01-foundation-metering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/default_config.py
  - tradingagents/graph/trading_graph.py
  - pyproject.toml
  - .env.example
  - tradingagents/revenium/__init__.py
  - tradingagents/revenium/config.py
  - scripts/setup_revenium.py
autonomous: false
requirements: [FND-01, FND-02, FND-03]
user_setup:
  - service: revenium
    why: "Metering and attribution hierarchy for the FCAT cost demo"
    env_vars:
      - name: REVENIUM_METERING_API_KEY
        source: "Revenium dashboard -> Settings -> API Keys -> metering key (rev_mk_*)"
      - name: REVENIUM_SK_API_KEY
        source: "Revenium dashboard -> Settings -> API Keys -> write/management key (rev_sk_*); provisioned now, consumed Phase 4 (D-10)"
    dashboard_config:
      - task: "Confirm/provision both rev_mk_ (metering) and rev_sk_ (write) keys; rev_mk_ is consumed this phase (D-10)"
        location: "Revenium dashboard -> API Keys"

must_haves:
  truths:
    - "A full propagate() run starts without an invalid-model-name API error (no gpt-5.5 / gpt-5.4-mini)"
    - "Deep-think agents run on Anthropic and quick-think agents run on OpenAI, producing two distinct provider labels"
    - "revenium-python-sdk[langchain] and revenium-metering are installed and pinned; archived revenium-middleware-langchain is absent"
    - "Running scripts/setup_revenium.py creates/verifies Organization, Subscriber, Product, and Subscription in Revenium"
    - "Re-running scripts/setup_revenium.py is idempotent (no duplicate entities)"
  artifacts:
    - path: "tradingagents/default_config.py"
      provides: "Valid demo model IDs + quick_think_provider split + revenium_* config keys and _ENV_OVERRIDES entries"
      contains: "quick_think_provider"
    - path: "tradingagents/revenium/config.py"
      provides: "revenium_task_type_map and revenium attribution defaults helper"
      contains: "revenium_task_type_map"
    - path: "scripts/setup_revenium.py"
      provides: "Idempotent Org->Subscriber->Product->Subscription provisioning (D-08)"
      contains: "def main"
    - path: "pyproject.toml"
      provides: "Pinned Revenium dependencies"
      contains: "revenium-python-sdk"
    - path: ".env.example"
      provides: "Documented Revenium env var placeholders (no real secrets)"
      contains: "REVENIUM_METERING_API_KEY"
  key_links:
    - from: "tradingagents/graph/trading_graph.py"
      to: "config quick_think_provider / deep_think_provider"
      via: "create_llm_client called twice with distinct providers"
      pattern: "create_llm_client"
    - from: "tradingagents/default_config.py"
      to: "REVENIUM_* env vars"
      via: "_ENV_OVERRIDES mapping + os.getenv defaults"
      pattern: "REVENIUM_METERING_API_KEY"
---

<objective>
Lay the prerequisite foundation for metering: fix the two day-one blockers (invalid default model names; missing Revenium attribution hierarchy), install and pin the Revenium SDK packages, register all `revenium_*` config keys, and provide an idempotent committed script that provisions the Revenium Organization → Subscriber → Product → Subscription before any metered call.

Purpose: Nothing can be metered correctly until valid models run, the SDK is installed, config keys exist, and the attribution hierarchy is created (attribution is NOT retroactive — events without identifiers land permanently in UNCLASSIFIED). This plan delivers the scaffolding the end-to-end metering slice (Plan 02) depends on.

Output: Patched `default_config.py` (valid models + provider split + revenium config keys), pinned deps in `pyproject.toml`, `.env.example` placeholders, the `tradingagents/revenium/` package skeleton (`__init__.py`, `config.py`), and `scripts/setup_revenium.py`.

Covers: FND-01 (model override + provider split), FND-02 (SDK install/pin), FND-03 (attribution hierarchy created before first metered call).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-metering/01-CONTEXT.md
@.planning/phases/01-foundation-metering/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix invalid model names and add the two-provider split (FND-01)</name>
  <files>tradingagents/default_config.py, tradingagents/graph/trading_graph.py</files>
  <read_first>
    - tradingagents/default_config.py (the DEFAULT_CONFIG dict literal at lines ~45-135 and _ENV_OVERRIDES at lines 10-21 — this is the source of truth for config keys)
    - tradingagents/graph/trading_graph.py lines 76-96 (the _get_provider_kwargs + two create_llm_client calls that currently both read self.config["llm_provider"])
    - tradingagents/llm_clients/factory.py (create_llm_client signature: provider, model, base_url, **kwargs — provider-neutral funnel)
    - .planning/phases/01-foundation-metering/01-PATTERNS.md section "tradingagents/default_config.py (edit — model fix L-03)" and section "tradingagents/graph/trading_graph.py"
    - .planning/research/PITFALLS.md Pitfall 9 (speculative default models)
  </read_first>
  <action>
    In tradingagents/default_config.py replace the invalid model defaults (`deep_think_llm` = `gpt-5.5`, `quick_think_llm` = `gpt-5.4-mini`) with valid demo IDs per L-03: `deep_think_llm` = `claude-sonnet-4-6`, `quick_think_llm` = `gpt-4.1-mini`. Because deep-think and quick-think must run on DIFFERENT providers for the cross-provider Revenium story (MTR-04, satisfied in Plan 03 but enabled here), add two new config keys: `deep_think_provider` defaulting to `anthropic` and `quick_think_provider` defaulting to `openai`. Keep the existing `llm_provider` key as the backward-compatible default that both new keys fall back to when unset. Add matching `_ENV_OVERRIDES` entries: `TRADINGAGENTS_DEEP_THINK_PROVIDER` -> `deep_think_provider`, `TRADINGAGENTS_QUICK_THINK_PROVIDER` -> `quick_think_provider`. In tradingagents/graph/trading_graph.py change the two create_llm_client calls so the deep client reads `self.config["deep_think_provider"]` and the quick client reads `self.config["quick_think_provider"]` instead of both reading `self.config["llm_provider"]`. Do NOT hardcode provider strings in trading_graph.py — read them from config (repo convention: no hardcoded providers in clients/agents). Pass the same `llm_kwargs` (including callbacks) to both calls unchanged. Verify no `gpt-5` string remains in default_config.py.
  </action>
  <verify>
    <automated>grep -rn "gpt-5" tradingagents/default_config.py; test -z "$(grep -rn 'gpt-5' tradingagents/default_config.py)" && echo PASS-no-gpt5 || echo FAIL</automated>
    <automated>grep -c "quick_think_provider" tradingagents/default_config.py</automated>
    <automated>.venv/bin/python -c "from tradingagents.default_config import DEFAULT_CONFIG as c; assert c['deep_think_llm']=='claude-sonnet-4-6'; assert c['quick_think_llm']=='gpt-4.1-mini'; assert c['deep_think_provider']=='anthropic'; assert c['quick_think_provider']=='openai'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "gpt-5" tradingagents/default_config.py returns nothing
    - DEFAULT_CONFIG["deep_think_llm"] == "claude-sonnet-4-6" and DEFAULT_CONFIG["quick_think_llm"] == "gpt-4.1-mini"
    - DEFAULT_CONFIG contains keys deep_think_provider (default "anthropic") and quick_think_provider (default "openai")
    - _ENV_OVERRIDES contains TRADINGAGENTS_DEEP_THINK_PROVIDER and TRADINGAGENTS_QUICK_THINK_PROVIDER entries
    - trading_graph.py deep create_llm_client reads config["deep_think_provider"] and quick reads config["quick_think_provider"]; no literal "anthropic"/"openai" string is hardcoded in trading_graph.py
  </acceptance_criteria>
  <done>A run reads two distinct providers from config; the invalid speculative model names are gone; provider split is wired without hardcoding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy gate for Revenium SDK installs</name>
  <files>pyproject.toml</files>
  <action>Present the two Revenium packages and their PyPI provenance to the human for legitimacy approval before any install runs. No code is written in this task; it gates the install in Task 3. This checkpoint is never auto-approvable.</action>
  <what-built>
    The two packages to install are pinned from RESEARCH STACK.md, which verified them against the PyPI JSON API (HIGH confidence): `revenium-python-sdk` (publisher: Revenium org on PyPI) and `revenium-metering` (Revenium org). The archived `revenium-middleware-langchain` is explicitly excluded (double-counting). Because these are package-manager installs, a human must confirm legitimacy before install (this gate is never auto-approvable).
  </what-built>
  <how-to-verify>
    1. Confirm `revenium-python-sdk` on https://pypi.org/project/revenium-python-sdk/ is published by the Revenium organization and is actively maintained (>=0.1.9).
    2. Confirm `revenium-metering` on https://pypi.org/project/revenium-metering/ is published by the same Revenium organization (>=6.8.2).
    3. Confirm you do NOT want `revenium-middleware-langchain` (archived; would double-count).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize installing revenium-python-sdk[langchain]>=0.1.9 and revenium-metering>=6.8.2, or name a package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install/pin Revenium SDK packages and add revenium config keys + .env.example (FND-02)</name>
  <files>pyproject.toml, tradingagents/revenium/__init__.py, tradingagents/revenium/config.py, tradingagents/default_config.py, .env.example</files>
  <read_first>
    - pyproject.toml lines 11-37 (the dependencies list and optional-dependencies)
    - .planning/research/STACK.md sections "Recommended Stack", "Installation", "What NOT to Use" (archived revenium-middleware-langchain must NOT be installed)
    - .planning/phases/01-foundation-metering/01-PATTERNS.md sections "tradingagents/revenium/config.py" and ".env.example (edit)" and "tradingagents/revenium/__init__.py"
    - tradingagents/default_config.py (where the revenium_* keys + _ENV_OVERRIDES entries land)
    - .env.example (existing format: one comment block per integration area)
    - .gitignore (confirm .env is ignored at line 151 — do NOT add real keys anywhere)
  </read_first>
  <action>
    Add to pyproject.toml [project.dependencies] the two pinned packages from L-02/STACK.md: `revenium-python-sdk[langchain]>=0.1.9` and `revenium-metering>=6.8.2`. Do NOT add `revenium-middleware-langchain` or any per-provider middleware (archived; double-counting per L-02 and PITFALLS Pitfall 1). Install into the existing .venv using uv. Create the `tradingagents/revenium/` package: `__init__.py` (module docstring; re-export placeholder, will export ReveniumCallbackHandler in Plan 02 — keep imports lazy/minimal now) and `config.py`. In default_config.py add the revenium_* keys per the PATTERNS map: `revenium_api_key` = os.getenv("REVENIUM_METERING_API_KEY", ""), `revenium_api_url` = os.getenv("REVENIUM_METERING_BASE_URL", "https://api.revenium.ai"), `revenium_organization_name` = "Revenium-Research-Desk" (D-01 — FLAG: ROADMAP planning-note B says "FCAT-Research-Desk" but CONTEXT D-01 is the locked authority = "Revenium-Research-Desk"; use the CONTEXT value), `revenium_product_name` = "trading-signal" (D-03), `revenium_subscriber_id` = "john.demic+trading@revenium.io" (D-02), and the `revenium_task_type_map` dict (D-11) mapping each of the 12 node names to its pipeline-stage task_type (`market_analyst`/`sentiment_analyst`/`news_analyst`/`fundamentals_analyst` -> `analysis`; `bull_researcher`/`bear_researcher` -> `research_debate`; `research_manager` -> `planning`; `trader` -> `trade`; `aggressive_debator`/`conservative_debator`/`neutral_debator` -> `risk_debate`; `portfolio_manager` -> `decision`). Add the five _ENV_OVERRIDES entries (REVENIUM_METERING_API_KEY, REVENIUM_METERING_BASE_URL, REVENIUM_ORGANIZATION_NAME, REVENIUM_PRODUCT_NAME, REVENIUM_SUBSCRIBER_ID). In tradingagents/revenium/config.py provide a small helper (e.g., `attribution_from_config(config) -> dict`) returning organizationName/productName/subscriber.id so callback/setup code shares one source of truth. Append the Revenium block to .env.example using only placeholders (empty REVENIUM_METERING_API_KEY, commented base URL, commented rev_sk_ var, commented identity overrides) — no secrets (D-09: credentials live in gitignored .env with documented placeholders in .env.example).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import importlib.metadata as m; print('sdk', m.version('revenium-python-sdk')); print('metering', m.version('revenium-metering'))"</automated>
    <automated>.venv/bin/python -c "import importlib.util,sys; sys.exit(0 if importlib.util.find_spec('revenium_middleware_langchain') is None else 1)" && echo PASS-no-archived || echo FAIL-archived-present</automated>
    <automated>.venv/bin/python -c "from tradingagents.default_config import DEFAULT_CONFIG as c; assert c['revenium_organization_name']=='Revenium-Research-Desk'; assert c['revenium_subscriber_id']=='john.demic+trading@revenium.io'; assert c['revenium_product_name']=='trading-signal'; assert c['revenium_task_type_map']['bull_researcher']=='research_debate'; assert c['revenium_task_type_map']['portfolio_manager']=='decision'; print('PASS')"</automated>
    <automated>grep -c "REVENIUM_METERING_API_KEY" .env.example</automated>
    <automated>grep -v '^#' .env.example | grep -c "rev_mk_\|rev_sk_\|hak_"</automated>
  </verify>
  <acceptance_criteria>
    - importlib.metadata reports revenium-python-sdk >=0.1.9 and revenium-metering >=6.8.2 installed
    - revenium_middleware_langchain is NOT importable (archived package absent)
    - DEFAULT_CONFIG["revenium_organization_name"] == "Revenium-Research-Desk" (CONTEXT D-01 authority, not ROADMAP's FCAT-Research-Desk)
    - revenium_task_type_map maps all 12 node names to the D-11 taxonomy values
    - .env.example contains REVENIUM_METERING_API_KEY= placeholder with no real key value (uncommented lines contain no rev_mk_/rev_sk_/hak_ literal)
    - tradingagents/revenium/__init__.py and tradingagents/revenium/config.py exist with module docstrings
  </acceptance_criteria>
  <done>SDK installed and pinned, archived package excluded, all revenium_* config keys + env overrides registered, .env.example documents placeholders with no secrets.</done>
</task>

<task type="auto">
  <name>Task 4: Idempotent Revenium attribution-hierarchy setup script (FND-03)</name>
  <files>scripts/setup_revenium.py</files>
  <read_first>
    - scripts/smoke_structured_output.py (the standalone-script structural blueprint: module docstring with Usage, argparse, def main() -> int, sys.exit(main()), PASS/FAIL print loop)
    - .planning/phases/01-foundation-metering/01-PATTERNS.md section "scripts/setup_revenium.py"
    - tradingagents/revenium/config.py (the attribution_from_config helper from Task 3 — reuse the D-01..D-03 values, do not re-hardcode them)
    - .planning/research/STACK.md section "Billing / Monetization" (REST endpoints /api/v2/cost-controls, /api/v2/credentials; ReveniumMetering client) and "Auth / Config"
    - .planning/research/PITFALLS.md Pitfall 7 (UNCLASSIFIED — hierarchy must exist before first metered call) and Pitfall 8 (rev_mk_ vs rev_sk_ key types)
  </read_first>
  <action>
    Create scripts/setup_revenium.py: an idempotent, committed, re-runnable provisioning script (D-08) that creates-or-verifies the Revenium Organization (`Revenium-Research-Desk`, D-01), Subscriber (`john.demic+trading@revenium.io`, D-02), Product (`trading-signal` priced $2.00/signal, D-03), and a Subscription linking Subscriber→Product. Read the four attribution values from tradingagents.revenium.config (the helper added in Task 3) — do not duplicate the literals. Read credentials from env (`REVENIUM_METERING_API_KEY`; if a write key is required for creation use `REVENIUM_SK_API_KEY` per D-10 — fail with a clear human-readable message if the needed key is absent or has the wrong prefix). Use the revenium-metering / revenium-python-sdk client (or documented REST endpoints) for create/verify. Idempotency: look up each entity by its natural key (name/email/product name) before creating; if found, verify and report "exists", else create and report "created". Follow the smoke-script shape: argparse (e.g., `--dry-run` to print intended actions without writing), `def main() -> int` returning 0 on success / 1 on first error, `if __name__ == "__main__": sys.exit(main())`, and a clear PASS/FAIL summary line. Wrap external calls so a failure prints a human-readable cause (not a raw traceback) and exits 1. NOTE: this script intentionally DOES write to Revenium (it provisions) — it is the documented exception to fail-open; the fail-open convention applies to the metering hot path (Plan 02), not to this setup utility. Never print or log the key material itself (log only symbolic names — repo convention).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast,sys; t=ast.parse(open('scripts/setup_revenium.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; sys.exit(0 if 'main' in names else 1)" && echo PASS-has-main || echo FAIL</automated>
    <automated>.venv/bin/python scripts/setup_revenium.py --dry-run; echo "exit=$?"</automated>
    <automated>grep -c "Revenium-Research-Desk\|trading-signal\|john.demic" scripts/setup_revenium.py; echo "note: values should be sourced from config helper, count may be 0 if fully delegated"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/setup_revenium.py defines main() -> int and uses sys.exit(main())
    - python scripts/setup_revenium.py --dry-run exits 0 and prints the intended Org/Subscriber/Product/Subscription actions without writing
    - Re-running the live script reports each entity as "exists" rather than creating duplicates (idempotent by natural key)
    - The script reads attribution values from tradingagents.revenium.config, not duplicated literals
    - The script never prints key material; on missing/wrong-prefix key it exits 1 with a human-readable message
  </acceptance_criteria>
  <done>A committed, version-controlled, idempotent script is the source of truth for the Revenium attribution hierarchy; --dry-run proves the intended actions; a live run creates the hierarchy before any metered call.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Verify the Revenium attribution hierarchy is provisioned</name>
  <files>scripts/setup_revenium.py</files>
  <action>Pause for the human to run the live setup_revenium.py against their Revenium account and confirm the Org/Subscriber/Product/Subscription exist and the re-run is idempotent. No code is written in this task; it verifies Tasks 1-4.</action>
  <what-built>
    Valid demo models + provider split, pinned Revenium SDK, all revenium_* config keys, .env.example placeholders, and the idempotent scripts/setup_revenium.py. Claude has run `scripts/setup_revenium.py --dry-run` (no writes). The live provisioning run and the real API keys require the human-controlled Revenium account.
  </what-built>
  <how-to-verify>
    1. Confirm `REVENIUM_METERING_API_KEY` (rev_mk_*) and `REVENIUM_SK_API_KEY` (rev_sk_*) are present in your local .env (NOT committed — verify `git status` shows no .env).
    2. Run the live provisioning: `.venv/bin/python scripts/setup_revenium.py` — expect it to report Organization "Revenium-Research-Desk", Subscriber "john.demic+trading@revenium.io", Product "trading-signal" ($2.00/signal), and a Subscription as created-or-exists, exiting 0.
    3. Re-run it once more — expect all four entities reported as "exists" (idempotent, no duplicates).
    4. In the Revenium dashboard, confirm the Organization, Subscriber, Product, and Subscription appear with the exact names above.
  </how-to-verify>
  <resume-signal>Type "approved" once the hierarchy is visible in Revenium and the re-run is idempotent, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → git history | `.env` (real keys) must never be committed; only `.env.example` placeholders enter git |
| app → Revenium API | `rev_mk_` / `rev_sk_` keys cross this boundary; key material must never be logged |
| setup script → Revenium account | The one place that WRITES to Revenium (provisioning); requires the write key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` containing rev_mk_/rev_sk_ keys | mitigate | `.env` already gitignored (line 151); only placeholders added to `.env.example`; verification grep asserts no rev_*/hak_ literal in uncommitted .env.example lines; checkpoint reminds operator to confirm `git status` shows no .env |
| T-01-02 | Information Disclosure | scripts/setup_revenium.py logging | mitigate | Script logs only symbolic entity names (org/subscriber/product), never key material (repo convention: log symbolic names only) |
| T-01-03 | Elevation of Privilege | rev_sk_ write key over-use | mitigate | Least-privilege per D-10: Phase 1 hot path consumes only rev_mk_; rev_sk_ used solely by the setup utility for provisioning; document the scope split |
| T-01-04 | Spoofing | Wrong key type (platform vs metering) → silent 401 | mitigate | Setup script validates key prefix (rev_mk_/rev_sk_) and exits 1 with a human-readable message on mismatch (PITFALLS Pitfall 8) |
| T-01-SC | Tampering | pip/uv installs of revenium-python-sdk, revenium-metering | mitigate | Packages pre-verified in RESEARCH STACK.md against the PyPI JSON API (Revenium publisher); Task 2 is a blocking-human legitimacy gate; archived revenium-middleware-langchain explicitly excluded (double-counting) |
</threat_model>

<verification>
- A full graph construction reads `deep_think_provider` (anthropic) and `quick_think_provider` (openai) from config without model-name errors.
- `revenium-python-sdk[langchain]` and `revenium-metering` import; `revenium_middleware_langchain` does not.
- All `revenium_*` config keys present with the locked D-01..D-03/D-11 values.
- `scripts/setup_revenium.py --dry-run` exits 0; live run + re-run is idempotent and visible in Revenium.
- `.env.example` carries placeholders only; `.env` remains gitignored.
</verification>

<success_criteria>
- FND-01: invalid model IDs replaced with valid demo IDs; deep-think on Anthropic, quick-think on OpenAI via config (no hardcoding).
- FND-02: Revenium SDK packages installed and pinned in pyproject.toml; archived package excluded.
- FND-03: idempotent committed setup_revenium.py creates the full attribution hierarchy before any metered call.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-metering/01-01-SUMMARY.md` when done.
</output>
