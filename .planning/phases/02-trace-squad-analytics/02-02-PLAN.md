---
phase: 02-trace-squad-analytics
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/validate_tracing.py
autonomous: false
requirements: [TRC-01, TRC-02, TRC-03]

user_setup:
  - service: revenium
    why: "Live span-count, dependency-tree, and circular-pattern confirmation requires a real metered run against the Revenium account"
    env_vars:
      - name: REVENIUM_METERING_API_KEY
        source: "Revenium dashboard -> API keys (same key used in Phase 1 validation)"
      - name: FRED_API_KEY
        source: "FRED API key (Phase 1 workaround — required for the full news/macro pipeline run)"
    dashboard_config:
      - task: "Open the trace detail for the printed trace_id and inspect the Transaction Timeline (Gantt), Dependency Tree, and Agent Interaction / Circular Pattern views"
        location: "Revenium dashboard -> Traces"

must_haves:
  truths:
    - "scripts/validate_tracing.py runs one full propagate(), flushes background threads, and prints trace_id= and span_count="
    - "A live run produces one trace grouping N spans (N recorded as the expected value for the demo)"
    - "The Revenium trace detail shows a per-agent Gantt timeline and a dependency tree with parent arrows (not a flat span list)"
    - "The debate loop surfaces as the cost hotspot — via circular-pattern detection (max_debate_rounds=2) or the documented task_type=research_debate fallback"
  artifacts:
    - path: "scripts/validate_tracing.py"
      provides: "Live end-to-end tracing validation: run propagate, capture trace_id, count spans, assert"
      min_lines: 80
  key_links:
    - from: "scripts/validate_tracing.py"
      to: "tradingagents.graph.trading_graph.TradingAgentsGraph"
      via: "propagate() (inner _run_graph owns revenium_run_context), then graph._revenium_handler._threads join; trace_id read from the first captured payload"
      pattern: "_revenium_handler"
---

<objective>
Build the live tracing validation script and confirm, against a real Revenium account, the three things only a live run can prove: the single-trace span count N (TRC-01), the dependency tree rendered from parent_transaction_id (TRC-02), and the debate-loop cost hotspot via circular-pattern detection or the documented task_type fallback (TRC-03).

Purpose: Plan 02-01 proves payload-field correctness keyless. This plan closes the loop on the open research questions — actual span count N, whether the dependency tree renders, and whether circular detection fires at max_debate_rounds=2 — and records the demo-reliable answer before Phase 3.

Output: scripts/validate_tracing.py plus a recorded expected span count and the chosen TRC-03 path (circular vs fallback), captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trace-squad-analytics/02-RESEARCH.md
@.planning/phases/02-trace-squad-analytics/02-PATTERNS.md
@.planning/phases/02-trace-squad-analytics/02-01-PLAN.md
</context>

<phase_goal>
A full propagate() run appears in Revenium as one trace with a per-agent Gantt timeline and the debate loops surfacing as the cost hotspot. Plan 02-01 made the payloads correct; this plan runs the real thing and confirms Revenium renders it as designed, recording the span count and TRC-03 path for the demo.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/validate_tracing.py (live tracing validator)</name>
  <files>scripts/validate_tracing.py</files>
  <read_first>
    - scripts/validate_metering.py (analog — module docstring 1-47; imports 49-54; _run_checks helper 56-66; main() structure 196-442: argparse with description=__doc__, load_dotenv inside main, dict(DEFAULT_CONFIG) base, api-key gate, handler/graph construction, thread-flush loop 357-359, _run_checks call, dashboard reminder block, sys.exit(main()))
    - tradingagents/graph/trading_graph.py (TradingAgentsGraph construction; self._revenium_handler at line 75; propagate() at 345; CRITICAL: _run_graph() already opens its own revenium_run_context() internally — confirm this so the validator does NOT add a second outer wrapper; the _revenium_handler._threads list to flush) — read-only confirmation, NOT modified by this plan
    - tradingagents/revenium/context.py (current_trace_id / revenium_run_context as modified in 02-01 — read to confirm the context manager is NOT re-entrant: it always sets a fresh uuid4() into current_trace_id, so an outer wrapper's id would be overridden by the inner _run_graph context. The validator reads trace_id from the captured payload, not from this context)
    - .planning/phases/02-trace-squad-analytics/02-RESEARCH.md (validate_tracing.py sketch in Code Examples; "Already in Place" table confirming _run_graph() owns revenium_run_context(); LangGraph Topology / span-count section; Validation Architecture)
    - .planning/phases/02-trace-squad-analytics/02-PATTERNS.md (section "scripts/validate_tracing.py" — what to copy verbatim vs replace)
  </read_first>
  <action>
    Create scripts/validate_tracing.py modeled on validate_metering.py. Copy the module-docstring style (purpose, usage with env vars, what to confirm in the dashboard after exit), the import block (from __future__ import annotations; argparse; sys; datetime), and the _run_checks helper verbatim. Defer all tradingagents.* imports into main().

    In main(): call load_dotenv() first; build config from dict(DEFAULT_CONFIG); set `config["max_debate_rounds"] = 2` so bull_researcher appears twice in the parent chain (the condition under which circular-pattern detection can fire — research A4); allow a `--ticker` (default NVDA) and `--date` (default 2026-06-27) argparse flag. Gate on `config["revenium_api_key"]`: if empty, print a clear "no REVENIUM_METERING_API_KEY — keyless mode, skipping live assertions" message and return 0 (so the script is safe to invoke keyless in CI).

    Construct `graph = TradingAgentsGraph(config=config)`. Before running, monkeypatch `graph._revenium_handler._client.meter_ai_completion` to append each payload to a local `captured` list (validate_metering.py capture pattern) so span_count is the exact number of emitted events and the distinct `agent` set can be asserted. Run `graph.propagate(ticker, date)` DIRECTLY — do NOT wrap it in `revenium_run_context(...)`. trading_graph._run_graph() already opens its own revenium_run_context() internally (research "Already in Place" table), and that context manager is not re-entrant: it always sets a fresh uuid4() into current_trace_id. An outer wrapper here would generate a second UUID that the inner context overrides, so the printed trace_id would never match the metered events (the original blocker). After propagate() returns, flush background threads via `for t in list(graph._revenium_handler._threads): t.join(timeout=5.0)`. Then derive `trace_id = captured[0].get("trace_id", "")` from the FIRST captured payload — this is the UUID the inner _run_graph context owned and that every metered event carries — and set `span_count = len(captured)`. Print `trace_id=<uuid>` and `span_count=<n>` on their own lines. Build a `checks` list[tuple[str,bool]]: trace_id is non-empty AND equals `captured[0]["trace_id"]`; every captured payload shares that same trace_id (single-trace invariant); span_count >= 12; every captured payload after the first has "parent_transaction_id"; all captured payloads carry "trace_name"/"trace_type"/"transaction_name". Run them through _run_checks and print a dashboard reminder block (open the trace_id, inspect Gantt + Dependency Tree + Circular Pattern; if circular not shown, filter analytics by task_type=research_debate). Return 0 on all-pass / keyless, 1 on any fail; `sys.exit(main())` at the bottom.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/validate_tracing.py').read()); print('parse-ok')"</automated>
    <automated>ruff check scripts/validate_tracing.py</automated>
    <automated>python scripts/validate_tracing.py --help</automated>
  </verify>
  <acceptance_criteria>
    - scripts/validate_tracing.py parses and `ruff check` exits 0
    - `python scripts/validate_tracing.py --help` exits 0 and shows --ticker and --date flags
    - run with no REVENIUM_METERING_API_KEY: prints a keyless-skip message and exits 0 without a traceback
    - the script sets config["max_debate_rounds"] = 2 before constructing the graph
    - the script does NOT open its own revenium_run_context() wrapper around propagate(); it relies on the inner _run_graph() context and reads trace_id from the first captured payload
    - the printed trace_id is non-empty and is asserted equal to captured[0]["trace_id"], so the printed id provably matches the metered events
    - the script flushes graph._revenium_handler._threads before computing span_count, then prints lines beginning `trace_id=` and `span_count=` on a live run
  </acceptance_criteria>
  <done>The validator runs a full propagate, captures and counts emitted spans, prints a trace_id that matches the metered events, and degrades gracefully keyless.</done>
</task>

<task type="auto">
  <name>Task 2: Live verification — record span count N and the TRC-03 path</name>
  <files>scripts/validate_tracing.py</files>
  <read_first>
    - scripts/validate_tracing.py (the validator built in Task 1)
    - .planning/phases/02-trace-squad-analytics/02-RESEARCH.md (Open Questions 1-3; "The Core Risk: parentTransactionId Behavior at Loopback Edges"; Validation Architecture — the two-path TRC-03 acceptance)
  </read_first>
  <action>
    Run the validator live against the Revenium account (requires REVENIUM_METERING_API_KEY and FRED_API_KEY in .env). Command: `python scripts/validate_tracing.py --ticker NVDA --date 2026-06-27`. Capture the printed `span_count=` value and record it as the expected N in the plan SUMMARY (do not hardcode an assertion before the first run — research instruction). In the Revenium dashboard, open the printed trace_id and confirm: (1) the Transaction Timeline / Gantt shows per-agent spans; (2) the Dependency Tree shows parent arrows between sequential agents, not a flat list; (3) the Agent Interaction view either shows a Circular Pattern entry for bull_researcher/bear_researcher (TRC-03 primary, since max_debate_rounds=2) OR, if it does not fire, confirm the task_type=research_debate cost bucket surfaces the debate loop as the hotspot (TRC-03 fallback). Record which TRC-03 path is demo-reliable and the answer to research Open Questions 1 (does trace_type create a squad view) and 3 (ingest latency) in the SUMMARY. If the dependency tree is flat (no arrows), that means the parent_transaction_id chain broke inside callback.py — file it as a blocker and re-check the synchronous current_parent_transaction_id.set() ordering in on_llm_end from 02-01 Task 2. Note: the printed trace_id is read from the first captured payload, so it provably matches the metered events — an empty or wrong dashboard trace is NOT a trace_id mismatch and is NOT a context-manager race; investigate the parent threading, not the trace_id.
  </action>
  <verify>
    <human-check>
      Live run completed and confirmed in the Revenium dashboard:
      1. Run `python scripts/validate_tracing.py --ticker NVDA --date 2026-06-27` with REVENIUM_METERING_API_KEY and FRED_API_KEY set; note the printed trace_id and span_count.
      2. Open the trace_id in Revenium -> Traces. Confirm the Gantt timeline shows per-agent spans.
      3. Confirm the Dependency Tree shows parent arrows (every span except the first has a parent).
      4. Confirm EITHER a Circular Pattern entry for bull/bear researchers OR the task_type=research_debate bucket as the cost hotspot.
      5. Reply with the span_count N, the chosen TRC-03 path, and whether a squad view appeared under trace_type, or describe what was missing.
    </human-check>
  </verify>
  <acceptance_criteria>
    - A live run printed a trace_id and a concrete span_count N (N recorded in the SUMMARY as the expected demo value)
    - The Revenium trace detail showed a per-agent Gantt timeline (TRC-01 satisfied)
    - The dependency tree showed parent arrows, not a flat list (TRC-02 satisfied)
    - The debate-loop hotspot was confirmed via circular-pattern detection OR the task_type=research_debate fallback, and which path is demo-reliable is recorded (TRC-03 satisfied)
    - Research Open Questions 1 (squad/trace_type) and 3 (ingest latency) are answered in the SUMMARY
  </acceptance_criteria>
  <done>TRC-01/02/03 confirmed live; span count N and the TRC-03 demo path recorded for Phase 3 / the demo narrative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate_tracing.py -> Revenium (api.revenium.ai) | A real metered run sends trace/transaction metadata to the external account |
| .env secrets -> validation process | REVENIUM_METERING_API_KEY / FRED_API_KEY loaded via load_dotenv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | validate_tracing.py stdout | mitigate | Print only the trace_id (a UUID) and span counts / agent names — never the API key or prompt content. Mirrors validate_metering.py's no-secret output discipline. |
| T-02-06 | Denial of Service | live propagate() in the validator | accept | The validator is an operator-run script, not in the trading hot path; the underlying metering remains fail-open. A Revenium outage causes failed checks, not a crashed run. |
| T-02-07 | Tampering | --ticker / --date CLI args | mitigate | Ticker flows through the existing safe_ticker_component sanitization and the 200-char trace_name cap from 02-01; date is a string label only. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase. No install task, no new supply-chain surface. |
</threat_model>

<verification>
- `python scripts/validate_tracing.py --help` exits 0; keyless invocation degrades gracefully (exit 0, no traceback).
- `ruff check scripts/validate_tracing.py` exits 0.
- Live: a real run prints trace_id and span_count; dashboard confirms Gantt + dependency tree + debate hotspot.
</verification>

<success_criteria>
- validate_tracing.py runs one full propagate(), flushes threads, and prints a trace_id (matching the metered events) and span_count, degrading gracefully without a key.
- A live run confirms one trace with N spans, a per-agent Gantt, a parent-arrow dependency tree, and the debate-loop hotspot (circular or fallback) — closing TRC-01, TRC-02, TRC-03.
- Span count N and the demo-reliable TRC-03 path are recorded for the demo narrative and Phase 3.
</success_criteria>

<output>
Create `.planning/phases/02-trace-squad-analytics/02-02-SUMMARY.md` when done — include the recorded span count N, the chosen TRC-03 path, and answers to research Open Questions 1 and 3.
</output>
