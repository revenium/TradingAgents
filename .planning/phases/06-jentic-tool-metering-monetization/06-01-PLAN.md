---
phase: 06-jentic-tool-metering-monetization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tradingagents/default_config.py
  - tests/conftest.py
  - .env.example
autonomous: false
requirements: [JEN-01]
must_haves:
  truths:
    - "`import jentic` succeeds from a clean `pip install \".[dev]\"` (declared, not just venv-installed)"
    - "Five `jentic_*` config keys resolve from DEFAULT_CONFIG with env-var overrides"
    - "The full unit suite stays green with JENTIC_AGENT_API_KEY unset AND when it is set in .env"
  artifacts:
    - path: "pyproject.toml"
      provides: "jentic>=0.10.0,<1.0 in [project].dependencies"
      contains: "jentic"
    - path: "tradingagents/default_config.py"
      provides: "jentic_tool_enabled / jentic_agent_api_key / jentic_op_id / jentic_search_query / jentic_tool_id + 5 _ENV_OVERRIDES entries"
      contains: "jentic_tool_enabled"
    - path: "tests/conftest.py"
      provides: "JENTIC_AGENT_API_KEY forced empty so keyless isolation holds even when .env sets it"
      contains: "JENTIC_AGENT_API_KEY"
  key_links:
    - from: "tradingagents/default_config.py _ENV_OVERRIDES"
      to: "DEFAULT_CONFIG jentic_* keys"
      via: "env-var -> config-key coercion loop"
      pattern: "JENTIC_TOOL_ENABLED"
---

<objective>
Establish the Phase 6 foundation: declare the `jentic` dependency (currently venv-only, undeclared — CI blocker L1), register the five `jentic_*` config keys with env overrides, and harden keyless test isolation for `JENTIC_AGENT_API_KEY`. No Jentic code runs yet; this plan makes the dependency real and the config surface exist.

Derived per-slice user story (ROADMAP Goal is outcome-form, not a user story — recommend `/gsd mvp-phase 6` to formalize; proceeding with a derived story per the Phase 4 precedent):
**As a** demo operator, **I want to** have `jentic` installable in CI and toggle the Jentic tool via config, **so that** the metered-tool slice can be built and tested without a live key.

Purpose: Remove the day-one CI blocker (jentic undeclared) and provide the config/secret surface every later task reads.
Output: Updated pyproject.toml + uv.lock, five config keys + overrides, conftest keyless guard, .env.example entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-jentic-tool-metering-monetization/06-CONTEXT.md
@.planning/phases/06-jentic-tool-metering-monetization/06-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm jentic package legitimacy before declaring it a dependency</name>
  <what-built>Nothing yet — this gate precedes adding `jentic` to pyproject. `jentic==0.10.0` is `[ASSUMED]` (slopcheck was unavailable in RESEARCH.md; official-SDK origin assumed but not machine-verified). Per the package-legitimacy protocol and threat T-06-SC, origin must be human-confirmed before the install task runs.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/jentic/ and confirm: the project is published by Jentic (project links point to github.com/jentic and/or docs.jentic.com), version 0.10.0 exists, and download/age signals look like a real maintained SDK (not a days-old typosquat).
    2. Confirm the installed venv copy matches: `.venv/bin/python -c "import jentic, importlib.metadata as m; print(m.version('jentic'))"` prints `0.10.0`.
    3. Confirm the linked source repo is the official Jentic SDK repo (github.com/jentic/jentic).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with declaring jentic as a dependency, or describe the concern (e.g. wrong publisher) to halt. This checkpoint is NOT auto-approvable.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Declare jentic dependency + regenerate lockfile</name>
  <read_first>
    - pyproject.toml (the `[project].dependencies` array near line 11; add alongside existing pins like `langchain-core>=0.3.81`)
    - .planning/phases/06-jentic-tool-metering-monetization/06-RESEARCH.md (Landmine L1, "Installation" block, Assumption A2 recommending the `<1.0` pin)
  </read_first>
  <files>pyproject.toml, uv.lock</files>
  <action>
    Add `"jentic>=0.10.0,<1.0"` to the `[project].dependencies` list in pyproject.toml (the upper bound follows RESEARCH.md A2 — the SDK is actively developed and the search/load/execute signatures are only confirmed for 0.10.x). Regenerate the lockfile with `uv lock` and confirm `jentic` now appears in uv.lock. Do NOT hand-edit uv.lock. Append near the other third-party pins. This closes L1 (ModuleNotFoundError in CI when `pip install ".[dev]"` skips the undeclared package).
  </action>
  <acceptance_criteria>
    - `grep -c 'jentic' pyproject.toml` is >= 1 and the entry is inside `[project].dependencies`.
    - `grep -c 'name = "jentic"' uv.lock` is >= 1 — the lock actually tracks it.
    - `.venv/bin/python -c "import jentic; print('ok')"` prints `ok`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import jentic; print('import-ok')" && grep -q 'name = "jentic"' uv.lock && echo lock-ok</automated>
  </verify>
  <done>jentic is a declared, locked dependency and imports cleanly; a fresh `pip install ".[dev]"` would install it.</done>
</task>

<task type="auto">
  <name>Task 3: Add five jentic_* config keys + env overrides + keyless conftest guard + .env.example</name>
  <read_first>
    - tradingagents/default_config.py (`_ENV_OVERRIDES` dict lines 10-38; the trailing Revenium keys block lines 186-209 — insert the jentic block right after `revenium_team_id`, before the closing `})`)
    - tests/conftest.py (lines 14-35: `_API_KEY_ENV_VARS` tuple + `_dummy_api_keys` autouse fixture — note it defaults absent vars to `"placeholder"`, which is WRONG for the jentic key: we need EMPTY so the config-gate returns the sentinel, see RESEARCH.md Pitfall 5 / L2)
    - .env.example (append a Phase 6 block mirroring the existing REVENIUM_* comment style)
    - .planning/phases/06-jentic-tool-metering-monetization/06-RESEARCH.md (Q5 exact key names/defaults; Q6 conftest gap; Pitfall 5)
  </read_first>
  <files>tradingagents/default_config.py, tests/conftest.py, .env.example</files>
  <action>
    In DEFAULT_CONFIG add exactly these five keys (JEN-01, per RESEARCH.md Q5): `jentic_tool_enabled` default `False` (bool literal so `_coerce` converts env `"true"/"1"` correctly); `jentic_agent_api_key` default `os.getenv("JENTIC_AGENT_API_KEY", "")`; `jentic_op_id` default `os.getenv("JENTIC_OP_ID", "op_ba86fdce1bade1b7")` (the CONTEXT-pinned NewsAPI getEverything op — account-specific, so it lives in config, never hardcoded in tool code; empty falls back to search); `jentic_search_query` default `os.getenv("JENTIC_SEARCH_QUERY", "get news headlines for a stock ticker")`; `jentic_tool_id` default `os.getenv("JENTIC_TOOL_ID", "jentic:news")` (single source of truth for the metered toolId AND the ToolResource registration — L6). Add matching `_ENV_OVERRIDES` entries: `JENTIC_TOOL_ENABLED -> jentic_tool_enabled`, `JENTIC_AGENT_API_KEY -> jentic_agent_api_key`, `JENTIC_OP_ID -> jentic_op_id`, `JENTIC_SEARCH_QUERY -> jentic_search_query`, `JENTIC_TOOL_ID -> jentic_tool_id`. Add a comment noting the pinned op-id is account-specific and config-driven (no provider hardcoding, per CLAUDE.md). In tests/conftest.py force `JENTIC_AGENT_API_KEY` to empty for every test so a developer's `.env` cannot leak a live key into the keyless suite: add a distinct autouse fixture (or a special-cased entry outside the `"placeholder"` loop) doing `monkeypatch.setenv("JENTIC_AGENT_API_KEY", "")`. In .env.example add `JENTIC_AGENT_API_KEY=`, `JENTIC_TOOL_ENABLED=false`, `JENTIC_OP_ID=op_ba86fdce1bade1b7`, `JENTIC_SEARCH_QUERY=`, `JENTIC_TOOL_ID=jentic:news` under a Phase 6 comment. Never log the key value anywhere (threat T-06-01).
  </action>
  <acceptance_criteria>
    - All five keys resolve from DEFAULT_CONFIG; `jentic_tool_enabled` is bool False; `jentic_op_id` defaults to `op_ba86fdce1bade1b7`.
    - Env override coerces: `JENTIC_TOOL_ENABLED=true` -> `jentic_tool_enabled is True` (bool, not the string).
    - conftest forces `JENTIC_AGENT_API_KEY=""` under test even when the shell/.env sets a value.
    - `.venv/bin/python -m pytest tests/ -m unit -q` is green.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "from tradingagents.default_config import DEFAULT_CONFIG as c; ks=['jentic_tool_enabled','jentic_agent_api_key','jentic_op_id','jentic_search_query','jentic_tool_id']; assert all(k in c for k in ks); assert c['jentic_tool_enabled'] is False; assert c['jentic_op_id']=='op_ba86fdce1bade1b7'; print('config-ok')" && .venv/bin/python -m pytest tests/ -m unit -q</automated>
  </verify>
  <done>All five jentic_* keys and their env overrides exist; keyless unit suite is green; the jentic key cannot leak from .env into tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env/.env -> process config | JENTIC_AGENT_API_KEY (a secret) crosses into DEFAULT_CONFIG at import |
| PyPI -> build/CI | third-party package `jentic` is installed into the runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | `pip`/`uv` install of `jentic` | mitigate | blocking-human legitimacy checkpoint (Task 1) before the install task; version pinned `>=0.10.0,<1.0` |
| T-06-01 | Information Disclosure | JENTIC_AGENT_API_KEY in config/.env | mitigate | key stored only in config/env; never logged; .env.example ships an empty placeholder; conftest forces empty under test |
| T-06-CI | Denial of Service (CI) | undeclared jentic dependency | mitigate | declare in pyproject + regenerate uv.lock so CI installs it (closes L1) |
</threat_model>

<verification>
- `.venv/bin/python -c "import jentic"` succeeds (declared + locked).
- Five jentic_* keys present with correct defaults and env coercion.
- `.venv/bin/python -m pytest tests/ -m unit -q` green with and without JENTIC_AGENT_API_KEY set.
</verification>

<success_criteria>
JEN-01 foundation delivered: jentic is a first-class declared/locked dependency, the five config keys + env overrides exist (with the pinned op-id in config, not code), and keyless test isolation is proven. No behavior change to the graph yet.
</success_criteria>

<output>
Create `.planning/phases/06-jentic-tool-metering-monetization/06-01-SUMMARY.md` when done.
</output>
