---
phase: 07-pilot-partner-integrations
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tradingagents/default_config.py
  - tradingagents/agents/utils/trinigence_tools.py
  - tradingagents/agents/utils/agent_utils.py
  - tradingagents/agents/analysts/market_analyst.py
  - scripts/setup_revenium.py
  - tests/test_trinigence_tool.py
autonomous: true
requirements: [PIL-02, PIL-04]
user_setup: []

must_haves:
  truths:
    - "With trinigence_tool_enabled=True a run emits exactly one trinigence_strategy Revenium tool event per call"
    - "get_trinigence_strategy returns a plausible NL→strategy output (strategy name / entry rules / exit rules / indicators)"
    - "The full test suite passes with NO API keys and NO network (trinigence_tool_enabled defaults False; mock is local)"
    - "The trinigence_strategy ToolResource is registered with per-call COUNT pricing via setup_revenium.py --partner-tools (upsert path)"
  artifacts:
    - path: "tradingagents/agents/utils/trinigence_tools.py"
      provides: "get_trinigence_strategy @tool + @meter_tool mock (config-gated, keyless, local)"
      contains: "meter_tool"
    - path: "tradingagents/default_config.py"
      provides: "trinigence_tool_enabled + trinigence_tool_id config keys + _ENV_OVERRIDES entries"
      contains: "trinigence_tool_id"
    - path: "scripts/setup_revenium.py"
      provides: "trinigence_strategy entry appended to _PARTNER_TOOLS registry"
      contains: "trinigence_tool_id"
  key_links:
    - from: "tradingagents/agents/analysts/market_analyst.py"
      to: "get_trinigence_strategy"
      via: "conditional append when cfg.get('trinigence_tool_enabled')"
      pattern: "trinigence_tool_enabled"
    - from: "tradingagents/agents/utils/trinigence_tools.py"
      to: "DEFAULT_CONFIG['trinigence_tool_id']"
      via: "@meter_tool single-source-of-truth toolId"
      pattern: "meter_tool\\(DEFAULT_CONFIG\\[.trinigence_tool_id.\\]\\)"
---

<objective>
Deliver Trinigence as a mocked, Revenium-metered AND priced NL→strategy-generation tool (PIL-02), cloning the Edgehound slice (07-01). End-to-end: config keys → local mock tool → wired into the market analyst → per-call COUNT price registered via the shared `--partner-tools` registry → keyless test.

Purpose: Second partner in the multi-partner FinOps story — a natural-language-to-trading-strategy generator, metered+priced identically, proving Revenium's neutral cost view across a second distinct partner shape.
Output: `get_trinigence_strategy` mock tool, config keys, market-analyst wiring, `trinigence_strategy` ToolResource registry entry, and a keyless unit test.
</objective>

<phase_goal>
**As a** Revenium FCAT demo operator, **I want to** run a ticker with Trinigence enabled and see a metered+priced `trinigence_strategy` tool event in Revenium, **so that** a second external partner (strategy-generation) shows up under the same neutral FinOps cost view — with no live Trinigence dependency.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Direct sibling analog — this slice mirrors it exactly
@.planning/phases/07-pilot-partner-integrations/07-01-PLAN.md

# Phase 6 analogs
@tradingagents/agents/utils/jentic_news_tools.py
@tradingagents/agents/analysts/news_analyst.py
@tests/test_jentic_tool.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trinigence config keys + local mock @meter_tool + agent_utils re-export</name>
  <files>tradingagents/default_config.py, tradingagents/agents/utils/trinigence_tools.py, tradingagents/agents/utils/agent_utils.py, tests/test_trinigence_tool.py</files>
  <read_first>
    - tradingagents/agents/utils/edgehound_tools.py (the sibling mock created in 07-01 — clone its structure exactly, changing domain to strategy-generation)
    - tradingagents/agents/utils/jentic_news_tools.py (canonical @tool/@meter_tool/config-gate/keyless pattern)
    - tradingagents/default_config.py (edgehound_* keys added in 07-01 — mirror for trinigence)
    - tradingagents/agents/utils/agent_utils.py (import + __all__ re-export hub)
    - tests/test_edgehound_tool.py (sibling test to clone)
  </read_first>
  <behavior>
    - Test: DEFAULT_CONFIG["trinigence_tool_id"] == "trinigence_strategy" and contains no ':'.
    - Test: with revenium_api_key set and _send_tool_event patched, get_trinigence_strategy.func(...) fires exactly one tool event whose tool_id == DEFAULT_CONFIG["trinigence_tool_id"].
    - Test: returned output contains strategy-generation fields (strategy_name, entry_rules, exit_rules, indicators).
    - Test: importing tradingagents.agents.utils.trinigence_tools with NO keys never raises.
  </behavior>
  <action>
    Add two keys to DEFAULT_CONFIG: `trinigence_tool_enabled` (bool literal `False`) and `trinigence_tool_id` (`os.getenv("TRINIGENCE_TOOL_ID", "trinigence_strategy")` — colon-free, single source of truth). Add `_ENV_OVERRIDES` rows `TRINIGENCE_TOOL_ENABLED`→`trinigence_tool_enabled` and `TRINIGENCE_TOOL_ID`→`trinigence_tool_id`.
    Create `tradingagents/agents/utils/trinigence_tools.py` cloning `edgehound_tools.py`: `@tool`-outermost, `@meter_tool(DEFAULT_CONFIG["trinigence_tool_id"])`-innermost function `get_trinigence_strategy(description: Annotated[str, "Natural-language description of the desired trading strategy"]) -> str`. `_trinigence_strategy_impl(description)` reads `get_config()` at call time and returns a deterministic plausible JSON string with keys: `strategy_name`, `entry_rules` (list), `exit_rules` (list), `indicators` (list), `backtest_summary`, `source: "trinigence (mock)"`. Fully local mock — no network, no key. Deterministic from the description so tests can assert structure.
    Re-export `get_trinigence_strategy` from `agent_utils.py` (import + add to `__all__`).
    Create `tests/test_trinigence_tool.py` (@pytest.mark.unit) cloning `test_edgehound_tool.py`: colon-free config toolId, exactly-one meter event with matching tool_id, strategy-field structure, import-never-raises.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_trinigence_tool.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_trinigence_tool.py -q` exits 0 with NO API keys set.
    - `python -c "from tradingagents.default_config import DEFAULT_CONFIG as c; assert c['trinigence_tool_id']=='trinigence_strategy' and ':' not in c['trinigence_tool_id']"` exits 0.
    - `python -c "from tradingagents.agents.utils.agent_utils import get_trinigence_strategy"` exits 0.
    - The meter-event test asserts `_send_tool_event` call_count == 1 with `tool_id == DEFAULT_CONFIG['trinigence_tool_id']`.
  </acceptance_criteria>
  <done>Trinigence config keys, local mock @meter_tool, and re-export exist; keyless test proves one metered event with a colon-free toolId and plausible strategy output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire get_trinigence_strategy into the market analyst (flag-gated) + append trinigence to _PARTNER_TOOLS</name>
  <files>tradingagents/agents/analysts/market_analyst.py, scripts/setup_revenium.py</files>
  <read_first>
    - tradingagents/agents/analysts/market_analyst.py (the call-time get_config() + edgehound conditional append added in 07-01 — add the trinigence branch alongside it)
    - tradingagents/agents/analysts/news_analyst.py lines 25-37 (canonical gating pattern)
    - scripts/setup_revenium.py (the _PARTNER_TOOLS registry + register_partner_tool + --partner-tools flag added in 07-01 — append the trinigence entry)
    - tradingagents/default_config.py (trinigence_tool_id key added in Task 1)
  </read_first>
  <behavior>
    - Test: when trinigence_tool_enabled=False, get_trinigence_strategy is NOT in the market analyst's tool list; when True, it IS.
    - Test (setup_revenium): `--partner-tools --dry-run` now prints intended registrations for BOTH edgehound_decision AND trinigence_strategy, each COUNT-priced.
  </behavior>
  <action>
    In `market_analyst.py`, add `if cfg.get("trinigence_tool_enabled"): tools.append(get_trinigence_strategy)` next to the existing edgehound branch (import `get_trinigence_strategy` from `tradingagents.agents.utils.agent_utils`). Do not alter the analyst prompt logic.
    In `scripts/setup_revenium.py`, append a SECOND entry to the existing `_PARTNER_TOOLS` registry: `{"tool_id_key": "trinigence_tool_id", "name": "Trinigence Strategy Generator", "description": "Mocked NL→strategy-generation partner tool, metered+priced per-call by Revenium (PIL-02).", "provider": "trinigence", "price_env": "TRINIGENCE_TOOL_PRICE", "default_price": "0.25"}`. No other setup_revenium.py changes — the `--partner-tools` handler and `register_partner_tool` already loop the registry (added in 07-01).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -q && REVENIUM_SK_API_KEY= .venv/bin/python scripts/setup_revenium.py --partner-tools --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/ -q` exits 0 with NO API keys and NO network (full suite green, DMO-04).
    - A test asserts `get_trinigence_strategy` absent from the market analyst tool list when disabled, present when enabled.
    - `REVENIUM_SK_API_KEY= .venv/bin/python scripts/setup_revenium.py --partner-tools --dry-run` exits 0 and output contains BOTH `edgehound_decision` and `trinigence_strategy` with `COUNT`.
    - `grep -q 'trinigence_tool_id' scripts/setup_revenium.py` succeeds.
  </acceptance_criteria>
  <done>Trinigence tool is conditionally wired into the market analyst; its colon-free `trinigence_strategy` ToolResource registers via the shared upsert-safe `--partner-tools` flag; full suite green keyless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup_revenium.py → Revenium API | Only network egress; offline registration, `rev_sk_` key. Runtime tool is a local mock. |
| config → @meter_tool decorator | toolId flows from config; wrong value mis-attributes cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | hardcoded toolId in trinigence tool code | mitigate | toolId sourced only from `DEFAULT_CONFIG['trinigence_tool_id']`; test asserts colon-free config value |
| T-07-05 | Elevation | tool offered to LLM when flag disabled | mitigate | call-time `cfg.get('trinigence_tool_enabled')` gate; test asserts absence when False |
| T-07-06 | Information disclosure | secret leaking via mock output/logs | accept | mock is fully local, takes no key, returns no secret |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies — stdlib + existing langchain only |
</threat_model>

<verification>
- Full suite green keyless: `.venv/bin/python -m pytest tests/ -q` exits 0.
- Metered event: trinigence test asserts exactly one `trinigence_strategy` tool event.
- Registration: `--partner-tools --dry-run` prints both edgehound_decision and trinigence_strategy COUNT-priced.
</verification>

<success_criteria>
- PIL-02 delivered: Trinigence mocked, metered (one `trinigence_strategy` event/call), priced (COUNT ToolResource), config-gated, keyless-mockable.
- PIL-04 (partial): colon-free `trinigence_strategy` registered via the shared `--partner-tools` upsert path; suite green with no keys/network.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-partner-integrations/07-02-SUMMARY.md` when done.
</output>
