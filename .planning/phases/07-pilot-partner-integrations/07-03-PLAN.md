---
phase: 07-pilot-partner-integrations
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - tradingagents/default_config.py
  - tradingagents/agents/utils/saif_gate.py
  - tradingagents/agents/utils/agent_states.py
  - tradingagents/graph/setup.py
  - tradingagents/graph/propagation.py
  - tradingagents/graph/trading_graph.py
  - scripts/setup_revenium.py
  - tests/test_saif_gate.py
  - tests/test_partner_integrations.py
autonomous: true
requirements: [PIL-03, PIL-04]
user_setup: []

must_haves:
  truths:
    - "SAIF is modeled as a safety/assurance GATE on the Portfolio Manager decision (pass/flag verdict), NOT a data tool"
    - "With saif_tool_enabled=True the graph runs a SAIF node after Portfolio Manager and emits exactly one saif_assurance Revenium tool event"
    - "With saif_tool_enabled=False the graph topology is unchanged (Portfolio Manager → END) and the existing structured-output flow is untouched"
    - "A single run with all three partners enabled emits 3 distinct partner tool events: edgehound_decision, trinigence_strategy, saif_assurance"
    - "The saif_assurance ToolResource is registered with per-call COUNT pricing via setup_revenium.py --partner-tools (upsert path); full suite passes with no keys/network"
  artifacts:
    - path: "tradingagents/agents/utils/saif_gate.py"
      provides: "run_saif_assurance @meter_tool mock gate + create_saif_gate_node factory"
      contains: "meter_tool"
    - path: "tradingagents/default_config.py"
      provides: "saif_tool_enabled + saif_tool_id config keys + _ENV_OVERRIDES entries"
      contains: "saif_tool_id"
    - path: "tests/test_partner_integrations.py"
      provides: "combined 3-distinct-tool-event assertion (Success Criterion 1)"
      contains: "saif_assurance"
  key_links:
    - from: "tradingagents/graph/setup.py"
      to: "create_saif_gate_node"
      via: "build-time conditional node + Portfolio Manager → SAIF → END edges when saif_tool_enabled"
      pattern: "saif_tool_enabled"
    - from: "tradingagents/agents/utils/saif_gate.py"
      to: "DEFAULT_CONFIG['saif_tool_id']"
      via: "@meter_tool single-source-of-truth toolId"
      pattern: "meter_tool\\(DEFAULT_CONFIG\\[.saif_tool_id.\\]\\)"
    - from: "tradingagents/agents/utils/saif_gate.py"
      to: "state['final_trade_decision']"
      via: "SAIF node inspects the rendered PM decision"
      pattern: "final_trade_decision"
---

<objective>
Deliver SAIF as a mocked, Revenium-metered AND priced safety/assurance GATE on the Portfolio Manager decision (PIL-03) — NOT a data tool. SAIF inspects the PM's rendered `PortfolioDecision` and returns a pass/flag governance verdict, metered per invocation. This slice also completes the phase: the shared `--partner-tools` registration for all three partners and a combined 3-distinct-tool-event integration test (PIL-04).

Purpose: The governance beat of the multi-partner FinOps story — Revenium meters+prices a safety/assurance gate the same way it meters data tools, and a single run shows three distinct partner tool events under one neutral cost view.
Output: `run_saif_assurance` metered mock gate, a SAIF graph node conditionally wired after the Portfolio Manager, config keys, `saif_assurance` ToolResource registration, a keyless gate test, and a combined 3-partner integration test.
</objective>

<phase_goal>
**As a** Revenium FCAT demo operator, **I want to** run a ticker with SAIF enabled and see a metered+priced `saif_assurance` gate verdict fire on the Portfolio Manager decision, **so that** the demo shows Revenium metering a governance/assurance partner (not just data tools) — completing 3 distinct partner tool events in one run, with no live SAIF dependency.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Sibling slices (tool shape + registry) — SAIF differs: it is a GATE, not an analyst tool
@.planning/phases/07-pilot-partner-integrations/07-01-PLAN.md
@.planning/phases/07-pilot-partner-integrations/07-02-PLAN.md

# Integration-point source
@tradingagents/agents/managers/portfolio_manager.py
@tradingagents/graph/setup.py
@tradingagents/agents/schemas.py
@tradingagents/revenium/meter_tool.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SAIF config keys + metered mock assurance gate + register saif_assurance ToolResource</name>
  <files>tradingagents/default_config.py, tradingagents/agents/utils/saif_gate.py, scripts/setup_revenium.py, tests/test_saif_gate.py</files>
  <read_first>
    - tradingagents/agents/utils/jentic_news_tools.py (the @meter_tool wrapping + config-gate + keyless-noop pattern)
    - tradingagents/revenium/meter_tool.py (how @meter_tool fires exactly one _send_tool_event, keyless no-op)
    - tradingagents/agents/schemas.py lines 170-227 (PortfolioDecision + render_pm_decision — the rendered markdown the gate inspects, e.g. "**Rating**:", "**Executive Summary**:")
    - scripts/setup_revenium.py (the _PARTNER_TOOLS registry + register_partner_tool + --partner-tools flag from 07-01 — append the saif entry)
    - tradingagents/default_config.py (edgehound_*/trinigence_* keys — mirror for saif)
  </read_first>
  <behavior>
    - Test: DEFAULT_CONFIG["saif_tool_id"] == "saif_assurance" and contains no ':'.
    - Test: run_saif_assurance is a GATE — given a rendered PM decision string it returns a verdict containing a PASS/FLAG status and check notes (not market/strategy data).
    - Test: with revenium_api_key set and _send_tool_event patched, calling the metered gate fires exactly one tool event whose tool_id == DEFAULT_CONFIG["saif_tool_id"].
    - Test: importing tradingagents.agents.utils.saif_gate with NO keys never raises.
    - Test (setup_revenium): `--partner-tools --dry-run` now prints all THREE toolIds including saif_assurance, COUNT-priced.
  </behavior>
  <action>
    Add two keys to DEFAULT_CONFIG: `saif_tool_enabled` (bool literal `False`) and `saif_tool_id` (`os.getenv("SAIF_TOOL_ID", "saif_assurance")` — colon-free, single source of truth). Add `_ENV_OVERRIDES` rows `SAIF_TOOL_ENABLED`→`saif_tool_enabled` and `SAIF_TOOL_ID`→`saif_tool_id`.
    Create `tradingagents/agents/utils/saif_gate.py`. Define `run_saif_assurance(decision_text: str) -> str` decorated `@meter_tool(DEFAULT_CONFIG["saif_tool_id"])` (this is the metered unit; it is NOT a LangChain `@tool` and is NOT re-exported through agent_utils — SAIF is a gate, not an analyst data tool). Its body is a LOCAL mock governance check: parse the rendered PM decision (look for the `**Rating**:` line via a simple substring/regex) and return a deterministic verdict JSON string with keys: `verdict` (`"PASS"` or `"FLAG"`), `checks` (list of named safety/assurance checks with pass/flag), `notes`, `source: "saif (mock)"`. Choose PASS/FLAG deterministically from the parsed rating (e.g. FLAG on `Sell`/`Underweight`, else PASS) so the demo has a plausible governance beat. No network, no key, never logs secrets.
    Append a THIRD entry to the existing `_PARTNER_TOOLS` registry in `scripts/setup_revenium.py`: `{"tool_id_key": "saif_tool_id", "name": "SAIF Safety & Assurance Gate", "description": "Mocked safety/assurance gate on the Portfolio Manager decision (pass/flag governance verdict), metered+priced per-call by Revenium (PIL-03).", "provider": "saif", "price_env": "SAIF_TOOL_PRICE", "default_price": "0.15"}`. No other setup_revenium.py changes.
    Create `tests/test_saif_gate.py` (@pytest.mark.unit): assert colon-free config toolId; assert the gate returns a PASS/FLAG verdict for a sample rendered PM decision; assert exactly one metered event with tool_id==config value (patch `revenium_metering.decorator._send_tool_event`); assert import-never-raises.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_saif_gate.py -q && REVENIUM_SK_API_KEY= .venv/bin/python scripts/setup_revenium.py --partner-tools --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_saif_gate.py -q` exits 0 with NO API keys set.
    - `python -c "from tradingagents.default_config import DEFAULT_CONFIG as c; assert c['saif_tool_id']=='saif_assurance' and ':' not in c['saif_tool_id']"` exits 0.
    - A test asserts `run_saif_assurance(<sample PM markdown>)` returns a verdict containing `PASS` or `FLAG` (governance verdict, not data).
    - The meter-event test asserts `_send_tool_event` call_count == 1 with `tool_id == DEFAULT_CONFIG['saif_tool_id']`.
    - `REVENIUM_SK_API_KEY= .venv/bin/python scripts/setup_revenium.py --partner-tools --dry-run` exits 0 and output contains `edgehound_decision`, `trinigence_strategy`, AND `saif_assurance`, each with `COUNT`.
  </acceptance_criteria>
  <done>SAIF config keys, a metered local mock assurance gate, and its `saif_assurance` ToolResource registry entry exist; keyless test proves one metered event, a PASS/FLAG verdict, and colon-free toolId; all three partner tools register via --partner-tools.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the SAIF gate node into the graph after Portfolio Manager (build-time flag-gated) + saif_verdict state field</name>
  <files>tradingagents/agents/utils/saif_gate.py, tradingagents/agents/utils/agent_states.py, tradingagents/graph/setup.py, tradingagents/graph/propagation.py, tradingagents/graph/trading_graph.py, tests/test_saif_gate.py</files>
  <read_first>
    - tradingagents/graph/setup.py lines 80-172 (node registration + the `workflow.add_edge("Portfolio Manager", END)` line at 170 — the insertion point)
    - tradingagents/agents/managers/portfolio_manager.py (the node returns final_trade_decision; do NOT change its structured-output flow — SAIF reads it downstream)
    - tradingagents/agents/utils/agent_states.py (AgentState TypedDict — add a saif_verdict field; see judge_decision at line 17 for the Annotated[str, ...] shape)
    - tradingagents/graph/propagation.py (Propagator.create_initial_state — add saif_verdict default)
    - tradingagents/graph/trading_graph.py (_log_state — include saif_verdict so the field is logged consistently; per CLAUDE.md a new AgentState field requires updating all three sites)
  </read_first>
  <behavior>
    - Test: when saif_tool_enabled=False, the compiled graph has NO "SAIF Assurance" node and Portfolio Manager still routes to END (topology unchanged; existing tests unaffected).
    - Test: when saif_tool_enabled=True, the compiled graph contains a "SAIF Assurance" node reachable after Portfolio Manager and before END.
    - Test: invoking the SAIF node factory with a state carrying final_trade_decision returns a {"saif_verdict": ...} delta and (with a key + patched _send_tool_event) fires one saif_assurance event.
  </behavior>
  <action>
    In `saif_gate.py`, add `create_saif_gate_node()` returning a LangGraph node `saif_gate_node(state) -> dict` that reads `state.get("final_trade_decision", "")`, calls `run_saif_assurance(decision_text)` (the metered gate from Task 1), and returns `{"saif_verdict": verdict}`. Set the Revenium agent name for attribution consistent with the other nodes (`current_agent_name.set("saif_assurance")` via `tradingagents.revenium.context`, matching the D-12 pattern). Fail-open: never raise out of the node.
    Add `saif_verdict: Annotated[str, "SAIF safety/assurance gate verdict on the PM decision"]` to the `AgentState` TypedDict in `agent_states.py`. Add `saif_verdict: ""` to the initial state in `propagation.py` `create_initial_state`. Include `saif_verdict` in `_log_state` in `trading_graph.py` (all three sites, per CLAUDE.md).
    In `setup.py`, BUILD-TIME gate the wiring (config is fixed at graph construction): read `get_config()` (or the setup's config), and if `saif_tool_enabled` is True, `workflow.add_node("SAIF Assurance", create_saif_gate_node())`, replace `workflow.add_edge("Portfolio Manager", END)` with `workflow.add_edge("Portfolio Manager", "SAIF Assurance")` + `workflow.add_edge("SAIF Assurance", END)`; otherwise keep the existing `Portfolio Manager → END` edge unchanged. Do NOT touch the Portfolio Manager node or its structured-output path.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/ -q` exits 0 with NO API keys and NO network (full suite green, DMO-04) — proves the disabled-default topology is unchanged.
    - A test builds the graph with `saif_tool_enabled=False` and asserts no `SAIF Assurance` node exists; with `True` asserts the node exists and Portfolio Manager routes to it.
    - A test asserts the SAIF node returns a `{"saif_verdict": ...}` delta and (with a key + patched `_send_tool_event`) fires exactly one `saif_assurance` event.
    - `grep -q 'saif_verdict' tradingagents/agents/utils/agent_states.py tradingagents/graph/propagation.py tradingagents/graph/trading_graph.py` succeeds (field plumbed at all three sites).
  </acceptance_criteria>
  <done>The SAIF gate runs as a graph node after the Portfolio Manager only when saif_tool_enabled=True, storing a saif_verdict without altering the structured-output flow; disabled default leaves topology and the full keyless suite unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Combined 3-partner integration test — 3 distinct tool events in one run (Success Criterion 1)</name>
  <files>tests/test_partner_integrations.py</files>
  <read_first>
    - tests/test_jentic_tool.py (the _send_tool_event capture + set_config pattern to reuse)
    - tests/test_edgehound_tool.py and tests/test_trinigence_tool.py (sibling assertions)
    - tests/test_saif_gate.py (SAIF gate invocation)
    - tradingagents/agents/utils/agent_utils.py (import get_edgehound_decision, get_trinigence_strategy)
    - tradingagents/agents/utils/saif_gate.py (import run_saif_assurance / create_saif_gate_node)
  </read_first>
  <behavior>
    - Test: with all three flags enabled and a revenium key set, exercising the three metered units (edgehound tool .func, trinigence tool .func, SAIF gate) with `_send_tool_event` patched captures exactly 3 tool events whose tool_ids are the set {edgehound_decision, trinigence_strategy, saif_assurance} — 3 DISTINCT partner tool events (Success Criterion 1).
    - Test: all three toolIds are colon-free.
    - Test: with all three flags default (False), the three metered units are not invoked in a normal analyst/graph path (config-gated) — asserted via the wiring tests already in place; this test focuses on the enabled-path event distinctness.
  </behavior>
  <action>
    Create `tests/test_partner_integrations.py` (@pytest.mark.unit). In one test: `set_config` to enable all three flags and set a `revenium_api_key` + attribution fields (mirror `test_jentic_tool.py`'s config block); patch `revenium_metering.decorator._send_tool_event` to collect calls; invoke `get_edgehound_decision.func(...)`, `get_trinigence_strategy.func(...)`, and `run_saif_assurance(<sample PM markdown>)`; assert the collected `tool_id` kwargs equal the set `{"edgehound_decision", "trinigence_strategy", "saif_assurance"}` and total call_count == 3. Add a second assertion that each toolId is sourced from its DEFAULT_CONFIG key and contains no ':'. Restore config in a finally block.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_partner_integrations.py -q && .venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_partner_integrations.py -q` exits 0 with NO API keys and NO network.
    - The test asserts exactly 3 captured tool events with distinct tool_ids `{edgehound_decision, trinigence_strategy, saif_assurance}`.
    - The test asserts all three toolIds are colon-free and sourced from their `DEFAULT_CONFIG` keys.
    - `.venv/bin/python -m pytest tests/ -q` exits 0 (full suite green keyless, DMO-04).
  </acceptance_criteria>
  <done>A single test proves 3 distinct partner tool events fire in one enabled run (Success Criterion 1), and the full suite stays green with no keys/network.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Portfolio Manager decision → SAIF gate | The gate reads the rendered PM decision (in-process, trusted state); it inspects, it does not mutate the decision. |
| setup_revenium.py → Revenium API | Only network egress; offline registration, `rev_sk_` key. SAIF gate runtime is a local mock. |
| config → graph topology | saif_tool_enabled controls whether the gate node is wired at build time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | SAIF gate mutating/blocking the PM decision | mitigate | node returns only `saif_verdict`; never edits `final_trade_decision`; test asserts PM→END topology unchanged when disabled |
| T-07-08 | Elevation | gate node wired when flag disabled | mitigate | build-time `saif_tool_enabled` gate in setup.py; test asserts no SAIF node by default |
| T-07-09 | Denial of service | gate exception breaking the run finalization | mitigate | node is fail-open (never raises); @meter_tool is fail-open + keyless no-op |
| T-07-10 | Tampering | hardcoded saif toolId | mitigate | toolId sourced only from `DEFAULT_CONFIG['saif_tool_id']`; test asserts colon-free config value |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies — stdlib + existing langgraph/langchain only |
</threat_model>

<verification>
- Full suite green keyless: `.venv/bin/python -m pytest tests/ -q` exits 0 (disabled-default topology unchanged).
- SAIF metered gate: test asserts exactly one `saif_assurance` event and a PASS/FLAG verdict.
- Combined: test asserts 3 distinct partner tool events {edgehound_decision, trinigence_strategy, saif_assurance}.
- Registration: `--partner-tools --dry-run` prints all three colon-free COUNT-priced ToolResources.
</verification>

<success_criteria>
- PIL-03 delivered: SAIF mocked as a metered+priced safety/assurance GATE (not a data tool) on the PM decision; config-gated; keyless-mockable; topology unchanged when disabled.
- PIL-04 completed: all three colon-free ToolResources register via the shared `--partner-tools` upsert path; a single run emits 3 distinct partner tool events; full suite green with no keys/network.
</success_criteria>

<output>
Create `.planning/phases/07-pilot-partner-integrations/07-03-SUMMARY.md` when done.
</output>
