---
phase: 03-cost-controls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup_revenium.py
  - .env.example
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CTL-04, CTL-03]
user_setup:
  - service: revenium
    why: "Provision the enforce-mode cost rule against the live Revenium management API"
    env_vars:
      - name: REVENIUM_SK_API_KEY
        source: "Revenium dashboard -> Settings -> API Keys (rev_sk_* write/management key)"
      - name: REVENIUM_TEAM_ID
        source: "Revenium dashboard URL or Settings -> Team (hashed team id, e.g. DZxzEl)"
    dashboard_config:
      - task: "Confirm the 'TradingAgents Demo Budget' rule shows shadowMode:false / enforce mode"
        location: "Revenium dashboard -> Guardrails -> Budget Rules"

must_haves:
  truths:
    - "Running scripts/setup_revenium.py creates or verifies the 'TradingAgents Demo Budget' rule in enforce mode (shadowMode:false) (CTL-04, D-06/D-07)"
    - "The setup is idempotent: re-running matches by name and PATCHes a shadow-mode rule to enforce, or no-ops if already enforce"
    - "The rule uses metricType:TOTAL_COST, windowType:DAILY, action:BLOCK, ORGANIZATION:IS:Revenium-Research-Desk filter, teamId in body (D-04)"
    - "An enforce-mode (non-shadow) breach produces an ENFORCEMENT_VIOLATION event visible in the dashboard (CTL-03)"
    - "REVENIUM_BYPASS=true is documented in .env.example and keeps the keyless suite green (D-10)"
    - "CTL-01 in REQUIREMENTS.md reflects the D-08 reword (full Revenium reliance, no in-process counter)"
  artifacts:
    - path: "scripts/setup_revenium.py"
      provides: "_setup_cost_rule() + _patch() helper, wired into main() after _setup_subscription"
      contains: "_setup_cost_rule"
    - path: ".env.example"
      provides: "Circuit-breaker env var documentation block"
      contains: "REVENIUM_CIRCUIT_BREAKER_ENABLED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CTL-01 reworded per D-08"
      contains: "circuit breaker gates the run"
  key_links:
    - from: "scripts/setup_revenium.py:main"
      to: "POST /ai/cost-controls"
      via: "_setup_cost_rule with ORGANIZATION filter + teamId in body"
      pattern: "/ai/cost-controls"
---

<objective>
Provision the demo's enforce-mode cost rule as version-controlled, idempotent infrastructure (D-06) and finalize the phase's configuration surface. Extend `scripts/setup_revenium.py` with `_setup_cost_rule()` that creates/verifies a `TOTAL_COST` DAILY `$1.00` BLOCK rule with `shadowMode:false` (CTL-04, D-04/D-07), document the circuit-breaker `.env` knobs (D-10), and finalize the CTL-01 reword (D-08).

Purpose: CTL-04 (rule confirmed in enforce mode) and CTL-03 (the enforce-mode rule is what produces the visible dashboard ENFORCEMENT_VIOLATION event during the run). Independent of the gate code in Plan 01 — runs in parallel (Wave 1).
Output: Modified setup script with the cost-rule provisioning, an updated `.env.example`, and the finalized CTL-01 requirement wording.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-controls/03-CONTEXT.md
@.planning/phases/03-cost-controls/03-RESEARCH.md
@.planning/phases/03-cost-controls/03-PATTERNS.md
@.planning/phases/01-foundation-metering/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _setup_cost_rule() + _patch() to setup_revenium.py</name>
  <files>scripts/setup_revenium.py</files>
  <read_first>
    - scripts/setup_revenium.py: import/constants header (ORG_NAME at ~81, PRODUCT_NAME at ~82); `_get` (~139), `_post` (~158), `_extract_list` (~184), `_handle_http_error` (~482); `_setup_product` idempotency pattern (~315-384); `main()` provisioning calls and `failures` accumulator (~575-596, the `_setup_subscription` call at ~593)
    - 03-PATTERNS.md → section `scripts/setup_revenium.py` (the `_patch` helper to add, the full `_setup_cost_rule` body, the DEMO_RULE_* constants, and the `main()` extension after `_setup_subscription`)
    - 03-RESEARCH.md §Code Examples #4 "Budget Rule Provisioning" + §"Budget Rule CREATE (management API)" (verified POST body) + §Common Pitfalls #4 (management host vs polling host) and #6 (teamId in body)
    - 03-CONTEXT.md D-04, D-06, D-07
  </read_first>
  <action>
    Add three module-level constants near ORG_NAME/PRODUCT_NAME: `DEMO_RULE_NAME = "TradingAgents Demo Budget"`, `DEMO_RULE_HARD_LIMIT = 1.00`, `DEMO_RULE_WARN_THRESHOLD = 0.50`. Add a `_patch(base_url, path, sk_key, payload)` helper alongside `_post` (same shape: requests.patch, `_headers(sk_key)`, `timeout=15`, raise_for_status, return json). Add `_setup_cost_rule(base_url, sk_key, team_id, dry_run) -> bool` mirroring the `_setup_product` idempotency structure: GET `/ai/cost-controls?teamId={team_id}` then `_extract_list` then match by `name == DEMO_RULE_NAME` (or `label`); if found and `shadowMode is False and enabled is True` print "exists in enforce mode" and return True; if found in wrong state PATCH `/ai/cost-controls/{rule_id}` with `{"shadowMode": False, "enabled": True}`; on 404 fall through to POST `/ai/cost-controls` with the verified body — `name`, `description`, `metricType:"TOTAL_COST"`, `windowType:"DAILY"`, `action:"BLOCK"`, `groupBy:"AGENT"`, `hardLimit:DEMO_RULE_HARD_LIMIT`, `warnThreshold:DEMO_RULE_WARN_THRESHOLD`, `shadowMode:False` (D-07 — this is the demo-stopper if true), `enabled:True`, `filters:[{"dimension":"ORGANIZATION","operator":"IS","value":ORG_NAME}]` (D-04: org-scoped, no credential filter so the compiled rule blocks all callers when breached), `teamId:team_id` (Pitfall #6 — MUST be in the body), `notificationChannelIds:[]`. Include a `dry_run` branch that prints the would-GET/POST/PATCH lines (mirror `_setup_product`). Use `_handle_http_error` for all HTTP errors (never logs the key). Wire into `main()` after the `_setup_subscription` call as step 5 with the same `failures += 1` guard. Use the existing `base_url` (management host `api.prod.ai.hcapp.io` — Pitfall #4: do NOT use the metering/polling host). Do NOT inline beyond the verified RESEARCH §Code Examples #4 body.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python scripts/setup_revenium.py --dry-run 2>&1 | grep -i "cost rule" && .venv/bin/ruff check scripts/setup_revenium.py</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/setup_revenium.py --dry-run` prints a "Cost rule: 'TradingAgents Demo Budget' (TOTAL_COST DAILY $1.0)" line and the would-GET/POST/PATCH dry-run lines, and exits 0 without any network write.
    - `grep -n "shadowMode" scripts/setup_revenium.py` shows the POST body sets `shadowMode: False` and the PATCH path sets `{"shadowMode": False, "enabled": True}`.
    - `grep -n '"teamId"' scripts/setup_revenium.py` confirms teamId is included in the cost-rule POST body.
    - `grep -n "/ai/cost-controls" scripts/setup_revenium.py` shows the management-host relative path (no `api.revenium.ai` literal added).
    - `ruff check scripts/setup_revenium.py` reports no new errors.
  </acceptance_criteria>
  <done>setup_revenium.py provisions the enforce-mode cost rule idempotently; dry-run prints the intended actions; PATCH/POST both force shadowMode:false.</done>
</task>

<task type="auto">
  <name>Task 2: Document CB env vars in .env.example + finalize CTL-01 reword</name>
  <files>.env.example, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .env.example lines 59-88 (the existing Revenium block: REVENIUM_METERING_API_KEY, REVENIUM_SK_API_KEY, REVENIUM_TEAM_ID, REVENIUM_PLATFORM_BASE_URL, demo identity vars)
    - .planning/REQUIREMENTS.md line 32 (CTL-01 — note: the reworded text + "(Reworded in Phase 3 discussion...)" note already appears here; this task confirms/finalizes it)
    - 03-CONTEXT.md D-08, D-09, D-10
    - 03-RESEARCH.md §Environment Availability (env var defaults and the REVENIUM_CB_POLL_INTERVAL_SECONDS=5 demo value)
  </read_first>
  <action>
    In `.env.example`, append a documented circuit-breaker block after the existing Revenium section (~line 87) covering, with one comment line each: `REVENIUM_CIRCUIT_BREAKER_ENABLED=` (set to `true` to arm enforcement; unset = no-op), `REVENIUM_CB_POLL_INTERVAL_SECONDS=` (note default 60 is too slow for demo; set to 5 — D-09), `REVENIUM_CB_FAIL_MODE=` (default `open` = fail-open when cache uninitialized), and `REVENIUM_BYPASS=` (set `true` to disable enforcement and keep the keyless test suite green — D-10/DMO-04). Note that REVENIUM_TEAM_ID (already present) doubles as the enforcement polling scope, and that the threshold value lives in the Revenium rule (provisioned by setup_revenium.py), NOT in .env. Keep all new keys commented-out. In `.planning/REQUIREMENTS.md`, verify CTL-01 (line ~32) matches the D-08 reword: "Revenium's circuit breaker gates the run ... raises BudgetExceededError when an enforce-mode cost rule is breached. Demo timing is made reliable operationally ... not via an in-process spend counter." If any in-process-counter / client-side-floor language remains in the CTL-01 line, replace it with the reworded form. Do NOT change CTL-02/03/04 wording. Do NOT add real secret values to .env.example.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && grep -c "REVENIUM_CIRCUIT_BREAKER_ENABLED\|REVENIUM_CB_POLL_INTERVAL_SECONDS\|REVENIUM_BYPASS\|REVENIUM_CB_FAIL_MODE" .env.example && grep -q "circuit breaker gates the run" .planning/REQUIREMENTS.md && echo "CTL-01 reword present"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` documents all four CB env vars (REVENIUM_CIRCUIT_BREAKER_ENABLED, REVENIUM_CB_POLL_INTERVAL_SECONDS, REVENIUM_CB_FAIL_MODE, REVENIUM_BYPASS), each commented-out with a one-line explanation, no real secret values.
    - REQUIREMENTS.md CTL-01 contains "circuit breaker gates the run" and "made reliable operationally", and contains NO surviving "in-process spend counter fires" client-side-floor wording in the CTL-01 line.
    - The keyless test suite remains green: `.venv/bin/python -m pytest -q` passes with no live keys.
  </acceptance_criteria>
  <done>.env.example documents the CB knobs (incl. REVENIUM_BYPASS for keyless tests); CTL-01 wording finalized per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup script → Revenium management API | `rev_sk_*` write key crosses to `api.prod.ai.hcapp.io` over TLS to create/patch the cost rule |
| .env.example → repo | Documentation of secret-bearing env var names (no values) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `_setup_cost_rule` HTTP error path | mitigate | Reuse `_handle_http_error` which never prints the `rev_sk_*` key; log only rule name / status / rule id |
| T-03-05 | Information Disclosure | .env.example committed to repo | mitigate | Only key NAMES and the documented enforce-mode posture are added; no real secret values — all keys commented-out and blank |
| T-03-06 | Elevation of Privilege | shadowMode misconfiguration | mitigate | POST forces `shadowMode:false` and the PATCH path repairs any shadow-mode rule to enforce (D-07) so the demo rule cannot silently observe-only |
| T-03-SC | Tampering | package installs | accept | No new packages this phase — no supply-chain checkpoint required |
</threat_model>

<verification>
- `python scripts/setup_revenium.py --dry-run` prints the cost-rule provisioning intent and exits 0 with no network write.
- `.env.example` documents the four CB env vars including REVENIUM_BYPASS.
- REQUIREMENTS.md CTL-01 reflects the D-08 reword.
- Full keyless suite green: `.venv/bin/python -m pytest -q`.
</verification>

<success_criteria>
- The enforce-mode cost rule is provisionable idempotently from the committed script (CTL-04, D-06/D-07).
- The rule's shadowMode:false posture is what makes the dashboard ENFORCEMENT_VIOLATION event fire visibly during the run (CTL-03).
- Configuration surface (.env.example) and the CTL-01 wording are finalized per D-08/D-09/D-10.
</success_criteria>

<output>
Create `.planning/phases/03-cost-controls/03-02-SUMMARY.md` when done.
</output>
