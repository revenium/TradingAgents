---
phase: 03-cost-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/revenium/callback.py
  - tests/test_revenium_enforcement.py
autonomous: true
requirements: [CTL-01, CTL-02]
user_setup: []

must_haves:
  truths:
    - "When a breached enforce-mode rule is cached, an LLM call raises BudgetExceededError from on_chat_model_start (CTL-01)"
    - "BudgetExceededError is NOT swallowed by the fail-open except block — it escapes the handler (D-03)"
    - "check_enforcement() is a no-op when REVENIUM_BYPASS=true or REVENIUM_CIRCUIT_BREAKER_ENABLED is unset (D-10)"
    - "shadowMode:true breached rules never block the run (D-07)"
    - "The keyless test suite passes with no live Revenium keys (DMO-04)"
  artifacts:
    - path: "tradingagents/revenium/callback.py"
      provides: "Enforcement gate in on_chat_model_start (check_enforcement before the try block)"
      contains: "check_enforcement"
    - path: "tests/test_revenium_enforcement.py"
      provides: "Keyless enforcement-gate unit tests (_seed_rules mock, propagation test)"
      contains: "BudgetExceededError"
  key_links:
    - from: "tradingagents/revenium/callback.py:on_chat_model_start"
      to: "revenium_middleware._core.check_enforcement"
      via: "pre-call gate placed BEFORE the fail-open try block"
      pattern: "check_enforcement\\("
---

<objective>
Deliver the core control mechanism (D-01): wire Revenium's `check_enforcement()` into `ReveniumCallbackHandler.on_chat_model_start` so a breached enforce-mode cost rule raises `BudgetExceededError`, and that exception deliberately escapes the handler's fail-open catch (D-03). This is the heart of the control pillar — Revenium itself stops the run.

Purpose: CTL-01 (gate raises in real time) and the handler-side half of CTL-02 (the error propagates out, ready for the CLI/propagate path to catch in Plan 03).
Output: Modified `callback.py` with the enforcement gate; new keyless test file `tests/test_revenium_enforcement.py` proving the gate raises, propagates, and is correctly no-op'd by bypass/disabled env.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-controls/03-CONTEXT.md
@.planning/phases/03-cost-controls/03-RESEARCH.md
@.planning/phases/03-cost-controls/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keyless enforcement-gate test scaffold (RED)</name>
  <files>tests/test_revenium_enforcement.py</files>
  <read_first>
    - tests/test_revenium_enforcement.py (does not exist yet — create it)
    - 03-RESEARCH.md §Code Examples #5 "Keyless Mock Pattern for Tests" (the `_reset_enforcement` autouse fixture, `_seed_rules`, and the four reference tests — VERIFIED locally)
    - 03-PATTERNS.md → section `tests/test_revenium_enforcement.py` (test class structure, `_reset_enforcement`, `_seed_rules`, `_make_serialized`, handler construction pattern)
    - tests/test_revenium_tracing.py (autouse fixture + handler construction analog)
    - tests/test_revenium_metering.py (handler construction pattern + `_make_serialized` analog, `@pytest.mark.unit` markers)
  </read_first>
  <behavior>
    - Test: check_enforcement() raises BudgetExceededError for a breached enforce-mode rule seeded into the cache (rule_name/threshold/current_value surfaced on the exception).
    - Test: REVENIUM_BYPASS=true short-circuits even when a breached rule is cached (no raise).
    - Test: shadowMode:true breached rule does not block (no raise).
    - Test: REVENIUM_CIRCUIT_BREAKER_ENABLED unset → check_enforcement() is a no-op (no raise).
    - Test (the RED one): BudgetExceededError raised by check_enforcement() propagates OUT of handler.on_chat_model_start (is NOT caught by the fail-open except). This fails until Task 2 wires the gate.
  </behavior>
  <action>
    Create `tests/test_revenium_enforcement.py` with an autouse `_reset_enforcement(monkeypatch)` fixture (delenv all CB env vars: REVENIUM_CIRCUIT_BREAKER_ENABLED, REVENIUM_BYPASS, REVENIUM_CB_FAIL_MODE, REVENIUM_TEAM_ID, REVENIUM_METERING_API_KEY; `importlib.reload(enforcement)`; `enforcement.stop_polling()` in yield cleanup) and a `_seed_rules(monkeypatch, rules, *, initialized=True)` helper that monkeypatches `enforcement._cached_rules`, `_cache_timestamp=inf`, `_cache_initialized`, and stubs `_ensure_poller_running`/`_fetch_rules` to no-op (mirror 03-RESEARCH.md §Code Examples #5 verbatim). Group tests into `TestEnforcementEngine` (the four direct check_enforcement tests) and `TestCallbackHandlerEnforcementGate` (the propagation test `test_budget_exceeded_propagates_from_on_chat_model_start`). Import `BudgetExceededError` from `revenium_middleware._core.exceptions` and `enforcement` from `revenium_middleware._core`. Construct the handler via the existing `ReveniumCallbackHandler` construction pattern from test_revenium_tracing.py (mock client, `enabled=True`, attribution dict with `subscriber_id`/`organizationName`/`productName`). Use `_make_serialized()` (copy from test_revenium_metering.py) for the on_chat_model_start serialized arg. Mark every test `@pytest.mark.unit`. The propagation test uses `with pytest.raises(BudgetExceededError): handler.on_chat_model_start(serialized, [], run_id="test-run-id")` — it MUST fail now (gate not yet wired) confirming RED. Honor D-10 keyless discipline: no live keys, all cache state seeded. Do NOT inline the full SDK source — only seed the cache fields named above.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python -m pytest tests/test_revenium_enforcement.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The four `TestEnforcementEngine` tests PASS (they exercise the SDK directly, already functional).
    - `test_budget_exceeded_propagates_from_on_chat_model_start` FAILS because the call returns instead of raising (RED confirmed — gate not yet wired). Failure mode is "DID NOT RAISE BudgetExceededError", NOT an import/collection error.
    - No test requires REVENIUM_METERING_API_KEY or any live key to run.
  </acceptance_criteria>
  <done>The test file exists, collects cleanly, the engine tests pass, and the propagation test fails for the right reason (gate absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire check_enforcement() gate into on_chat_model_start (GREEN)</name>
  <files>tradingagents/revenium/callback.py</files>
  <read_first>
    - tradingagents/revenium/callback.py lines 291-333 (the existing `on_chat_model_start`: the `if not self.enabled: return` guard at ~303, the `try:` at ~305, the fail-open `except Exception` at ~329) and the import block lines 61-79
    - 03-PATTERNS.md → section `tradingagents/revenium/callback.py` (exact insertion point: BETWEEN the `if not self.enabled: return` guard and the `try:` block; the import line to add)
    - 03-RESEARCH.md §Code Examples #1 "Enforcement Gate in on_chat_model_start" and §Common Pitfalls #1 "BudgetExceededError Swallowed by Fail-Open Block"
    - 03-CONTEXT.md D-01, D-02, D-03
  </read_first>
  <action>
    Add the module-level import `from revenium_middleware._core import BudgetExceededError, check_enforcement  # noqa: F401` to the callback.py import block (D-02: import the enforcement engine ONLY — this does NOT patch providers, verified; no double-counting with the Phase 1 metering path). Insert the enforcement gate in `on_chat_model_start` AFTER the `if not self.enabled: return` guard and BEFORE the `try:` block (D-03: the gate must sit OUTSIDE the fail-open try so `BudgetExceededError`, a direct `Exception` subclass, is NOT swallowed — this is the ONE deliberate exception to the repo's fail-open convention, the D-06 carry-forward from Phase 1). The gate is a single call: `check_enforcement({"subscriber_credential": self._attribution.get("subscriber_id", "")})`. Add a comment explaining D-03 and that it is a no-op when REVENIUM_CIRCUIT_BREAKER_ENABLED is unset or REVENIUM_BYPASS=true (keyless suite stays green — D-10/DMO-04). Do NOT modify the existing try/except capture code. Do NOT log the `subscriber_credential` value (it is a PII email — see threat_model T-03-02). Follow the verified excerpt in RESEARCH §Code Examples #1; no other implementation.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python -m pytest tests/test_revenium_enforcement.py -q 2>&1 | tail -15 && .venv/bin/ruff check tradingagents/revenium/callback.py</automated>
  </verify>
  <acceptance_criteria>
    - All tests in tests/test_revenium_enforcement.py PASS, including `test_budget_exceeded_propagates_from_on_chat_model_start` (GREEN).
    - `grep -n "check_enforcement(" tradingagents/revenium/callback.py` shows the call positioned before the `try:` in `on_chat_model_start` (not inside the try block).
    - `ruff check tradingagents/revenium/callback.py` reports no new errors.
    - The full keyless suite still passes: `.venv/bin/python -m pytest -q` is green with no live Revenium keys.
  </acceptance_criteria>
  <done>check_enforcement() is wired as a pre-call gate outside the fail-open block; BudgetExceededError propagates out of the handler; all keyless tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TradingAgents process → Revenium enforcement cache | `check_enforcement()` reads in-memory rules populated by the SDK daemon poller (no network in hot path) |
| usage_metadata → enforcement engine | `subscriber_credential` (a PII subscriber email) is passed into `check_enforcement()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | `on_chat_model_start` enforcement gate | mitigate | `subscriber_credential` (PII email) is passed to check_enforcement but MUST NOT be logged anywhere in callback.py; it travels only over TLS to Revenium (repo convention: log symbolic names only) |
| T-03-03 | Tampering / DoS | enforcement bypass env | accept | `REVENIUM_BYPASS=true` disables the gate by design (keyless tests, fail-open posture). Acceptable: env-controlled, demo operator owns the env |
| T-03-04 | Denial of Service | fail-open catch swallowing the gate | mitigate | Gate placed OUTSIDE the try block (D-03) so a real breach halts; test `test_budget_exceeded_propagates_from_on_chat_model_start` guards against regression |
| T-03-SC | Tampering | package installs | accept | No new packages this phase (enforcement engine already in `.venv` from Phase 1) — no supply-chain checkpoint required |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_revenium_enforcement.py -q` is green (all engine + propagation tests pass).
- `.venv/bin/python -m pytest -q` (full suite) is green with NO live Revenium keys set.
- `grep -n "check_enforcement(" tradingagents/revenium/callback.py` confirms the gate precedes the `try:` in on_chat_model_start.
</verification>

<success_criteria>
- A seeded-breach keyless test proves `BudgetExceededError` escapes `on_chat_model_start` (CTL-01, CTL-02 handler half).
- Bypass and shadow-mode and disabled-CB paths are all proven no-op (D-07, D-10).
- The keyless suite passes without live keys (DMO-04 discipline upheld for this phase).
</success_criteria>

<output>
Create `.planning/phases/03-cost-controls/03-01-SUMMARY.md` when done.
</output>
