---
phase: 03-cost-controls
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - cli/main.py
  - tradingagents/graph/trading_graph.py
  - scripts/validate_controls.py
  - tests/test_revenium_enforcement.py
autonomous: true
requirements: [CTL-02, CTL-03, CTL-01]
user_setup: []

must_haves:
  truths:
    - "When BudgetExceededError propagates out of the stream loop, the CLI renders a Rich halt panel with rule_name, spent/limit, resets_at, rule_id, and a per-agent token breakdown (CTL-02, D-05)"
    - "The CLI halt path exits non-zero (typer.Exit(code=1)) and prints NO BUY/HOLD/SELL decision (D-05)"
    - "The enforcement daemon thread is stopped on BOTH teardown paths: _run_graph finally (after end_run) and run_analysis finally"
    - "validate_controls.py runs a timed propagate(), reports which agent triggered the halt and elapsed seconds, and exits 0 in keyless mode"
    - "The halt panel never prints raw API key values (logs only rule name / cost / rule id)"
  artifacts:
    - path: "cli/main.py"
      provides: "_render_budget_halt_panel + try/except BudgetExceededError + finally stop_polling in run_analysis"
      contains: "_render_budget_halt_panel"
    - path: "tradingagents/graph/trading_graph.py"
      provides: "stop_polling() in _run_graph finally after end_run()"
      contains: "stop_polling"
    - path: "scripts/validate_controls.py"
      provides: "Timing dry-run + enforcement readiness validation script"
      contains: "BudgetExceededError"
  key_links:
    - from: "cli/main.py:run_analysis"
      to: "cli/main.py:_render_budget_halt_panel"
      via: "except BudgetExceededError around the graph.graph.stream loop"
      pattern: "_render_budget_halt_panel\\("
    - from: "tradingagents/graph/trading_graph.py:_run_graph"
      to: "revenium_middleware._core.stop_polling"
      via: "finally block after end_run()"
      pattern: "stop_polling\\(\\)"
---

<objective>
Complete the control-pillar vertical slice: catch the `BudgetExceededError` that Plan 01's gate raises and turn it into the on-stage demo moment (D-05). The CLI renders a graceful Rich halt panel with cost context and per-agent breakdown, exits non-zero, and prints no fabricated trading decision. Wire `stop_polling()` into both teardown paths so the enforcement daemon never outlives a run, and ship `validate_controls.py` for the timing dry-run that de-risks the live demo (D-09).

Purpose: CTL-02 (graceful caught halt with cost context), CTL-03 (validation confirms the dashboard enforcement event), and the live-path completion of CTL-01. Depends on Plan 01 (the gate must raise the error this plan catches).
Output: CLI halt panel + non-zero exit, stop_polling teardown in both paths, a new `validate_controls.py` timing script, and a keyless panel-rendering test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cost-controls/03-CONTEXT.md
@.planning/phases/03-cost-controls/03-RESEARCH.md
@.planning/phases/03-cost-controls/03-PATTERNS.md
@.planning/phases/03-cost-controls/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI halt panel + non-zero exit + CLI stop_polling teardown</name>
  <files>cli/main.py, tests/test_revenium_enforcement.py</files>
  <read_first>
    - cli/main.py: imports at top (lines 8-19 incl. `import typer`, Rich Panel/Table/Text/Live/Console); `def run_analysis` (~1022); the `with Live(layout, refresh_per_second=4):` block (~1131) and the `for chunk in graph.graph.stream(init_agent_state, **args):` loop (~1181); the existing `display_complete_report` Rich helper (~816/831) as the Panel/Table style analog
    - 03-PATTERNS.md → section `cli/main.py` (the try/except BudgetExceededError + finally stop_polling wrapper; the full `_render_budget_halt_panel` body; BudgetExceededError field reference; placement as a module-level helper before get_user_selections)
    - 03-RESEARCH.md §Code Examples #3 "CLI Halt Panel in run_analysis" + §Architecture Patterns (BudgetExceededError caught AFTER the Live context exits) + §Security Domain (never print key values)
    - tests/test_revenium_enforcement.py (extend with a panel-render test class — file owned by Plan 01, this plan is wave 2 so the extension is sequenced safely)
    - 03-CONTEXT.md D-05
  </read_first>
  <behavior>
    - Test: `_render_budget_halt_panel(console, err, handler)` renders without raising, given a constructed BudgetExceededError (rule_name/current_value/threshold/resets_at/rule_id) and a handler whose `agent_costs` holds a couple of agents.
    - Test: the rendered output (captured via a recording Console) contains the rule name, the spent and limit values, and the per-agent rows, and does NOT contain any `rev_mk_`/`rev_sk_` substring (no key leakage — T-03-01).
  </behavior>
  <action>
    Add `from revenium_middleware._core import BudgetExceededError, stop_polling` to the `run_analysis` setup (after the existing Revenium handler setup). Wrap the existing `with Live(layout, refresh_per_second=4):` streaming block in a `try:` and add `except BudgetExceededError as err:` that calls `_render_budget_halt_panel(console, err, revenium_handler)` then `raise typer.Exit(code=1)` (D-05: non-zero exit, no BUY/HOLD/SELL printed — the except handler runs AFTER the Live context has exited per RESEARCH §Architecture Patterns). Add a `finally:` that calls `stop_polling()` unconditionally (Pitfall #3 — the CLI stream path bypasses _run_graph, so its daemon teardown must live here; safe no-op if never started). Do NOT alter the existing chunk-processing/merge-final-state code inside the Live block. Add a module-level `_render_budget_halt_panel(console, err, handler)` helper (place near display helpers, before `get_user_selections`) that builds a Rich `Table` of per-agent input/output tokens from `handler.agent_costs`, a `Text` body with Rule / Spent / Limit / Resets at / Rule ID from the BudgetExceededError fields, prints them inside a red `Panel` titled "Run Halted — Budget Limit Reached", and prints a dashboard reminder line (Guardrails > Enforcement Events) plus a "non-zero status (no trading decision produced)" line. It MUST format only `err.rule_name`/`err.current_value`/`err.threshold`/`err.resets_at`/`err.rule_id` — never any key value (T-03-01). Follow RESEARCH §Code Examples #3 verbatim for the panel body. Extend `tests/test_revenium_enforcement.py` with a `TestBudgetHaltPanel` class implementing the two behaviors above (construct BudgetExceededError directly; use a `rich.console.Console(record=True)` and assert via `console.export_text()`). Mark tests `@pytest.mark.unit`.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python -m pytest tests/test_revenium_enforcement.py -q 2>&1 | tail -15 && .venv/bin/ruff check cli/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `TestBudgetHaltPanel` tests pass: the panel renders, contains the rule name + spent + limit + per-agent rows, and contains no `rev_mk_`/`rev_sk_` substring.
    - `grep -n "except BudgetExceededError" cli/main.py` shows the catch wraps the stream loop, followed by `typer.Exit(code=1)`.
    - `grep -n "stop_polling()" cli/main.py` shows the call in a `finally` clause in run_analysis.
    - The halt path renders NO BUY/HOLD/SELL — `_render_budget_halt_panel` contains no decision-printing call.
    - `ruff check cli/main.py` reports no new errors.
  </acceptance_criteria>
  <done>The CLI catches BudgetExceededError, renders the cost-context halt panel, exits non-zero with no fabricated decision, and tears down the enforcement daemon on every exit path.</done>
</task>

<task type="auto">
  <name>Task 2: stop_polling() teardown in _run_graph (propagate path)</name>
  <files>tradingagents/graph/trading_graph.py</files>
  <read_first>
    - tradingagents/graph/trading_graph.py: the `_run_graph` `finally` block (lines 470-474) where `self._revenium_handler.end_run()` already runs; the existing `from tradingagents.revenium.callback import ReveniumCallbackHandler` import line
    - 03-PATTERNS.md → section `tradingagents/graph/trading_graph.py` (the import to add; the ordering constraint end_run() BEFORE stop_polling())
    - 03-RESEARCH.md §Code Examples #2 "stop_polling() in _run_graph Finally Block" + §Anti-Patterns ("Calling stop_polling() before end_run()")
  </read_first>
  <action>
    Add `from revenium_middleware._core import stop_polling` after the existing `from tradingagents.revenium.callback import ReveniumCallbackHandler` import. In the `_run_graph` `finally` block, add `stop_polling()` AFTER the existing `self._revenium_handler.end_run()` call (ordering constraint: state-clear first, daemon-teardown second — RESEARCH §Anti-Patterns). Add a short comment noting it covers the propagate() path and that _ensure_poller_running() restarts the thread on the next run's first check_enforcement(). Both calls stay in `finally` so they run even when BudgetExceededError propagates through the try block. Do NOT change end_run() or any other finally logic. No other edits.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && grep -n "stop_polling" tradingagents/graph/trading_graph.py && .venv/bin/python -m pytest -q 2>&1 | tail -8 && .venv/bin/ruff check tradingagents/graph/trading_graph.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "stop_polling" tradingagents/graph/trading_graph.py` shows the import and a call inside the `_run_graph` finally block, positioned after `end_run()`.
    - The full keyless suite passes: `.venv/bin/python -m pytest -q` is green with no live keys.
    - `ruff check tradingagents/graph/trading_graph.py` reports no new errors.
  </acceptance_criteria>
  <done>The propagate() path stops the enforcement daemon in its finally block after end_run(), so the thread never outlives a run.</done>
</task>

<task type="auto">
  <name>Task 3: validate_controls.py timing dry-run script</name>
  <files>scripts/validate_controls.py</files>
  <read_first>
    - scripts/validate_tracing.py (full skeleton: module docstring, `_run_checks` helper at ~58-68, `main()` structure ~71-277, keyless gate ~109-113)
    - scripts/validate_metering.py (keyless-gate analog ~258-265)
    - 03-PATTERNS.md → section `scripts/validate_controls.py` (the docstring, `_run_checks` verbatim, the `main()` skeleton with timed propagate(), the BudgetExceededError capture, the checks list, the dashboard reminder, and the additional `import time` / `from datetime import datetime, timezone`)
    - 03-RESEARCH.md §Open Questions OQ-5 (timing guidance) + §Code Examples (BudgetExceededError fields)
    - 03-CONTEXT.md D-09
  </read_first>
  <action>
    Create `scripts/validate_controls.py` modeled on `validate_tracing.py`. Module docstring documents the live requirements (REVENIUM_METERING_API_KEY rev_mk_*, REVENIUM_CIRCUIT_BREAKER_ENABLED=true, REVENIUM_TEAM_ID, REVENIUM_CB_POLL_INTERVAL_SECONDS=5, an LLM key) and the keyless-mode behavior. Copy `_run_checks` verbatim. `main()`: argparse with `--ticker` (default NVDA) and `--date` (default 2026-06-27); `load_dotenv()`; build `config` from DEFAULT_CONFIG; KEYLESS GATE — if no `revenium_api_key` OR `REVENIUM_CIRCUIT_BREAKER_ENABLED` not truthy, print a keyless-mode message and `return 0` (DMO-04: CI never breaks on missing creds). Otherwise construct `TradingAgentsGraph(config=config)`, record `start_time = time.time()`, run `graph.propagate(args.ticker, args.date)` inside `try/except BudgetExceededError as err` capturing the exception and `elapsed_to_halt`; also catch generic Exception fail-open and surface in checks. Build a checks list: BudgetExceededError raised (run halted), rule_name non-empty, current_value > 0, threshold > 0, resets_at non-empty, halted within 120s. Call `_run_checks`, print a dashboard reminder block (Guardrails > Enforcement Events: action=BLOCK, ruleName, isShadow=false confirming CTL-04, currentValue > threshold, elapsed seconds and which agent — report `err` fields), and return 0 if no failures else 1 (D-09: the stop-watch dry-run reports where in the run the halt fires). Follow PATTERNS.md skeleton; do NOT print any API key value beyond the existing `api_key[:12]...` masked style used by the analogs.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python scripts/validate_controls.py 2>&1 | tail -5 && echo "exit=$?" && .venv/bin/ruff check scripts/validate_controls.py</automated>
  </verify>
  <acceptance_criteria>
    - Running `python scripts/validate_controls.py` with NO live keys prints a keyless-mode message and exits 0 (DMO-04).
    - `grep -n "BudgetExceededError" scripts/validate_controls.py` confirms the script captures the halt exception and reports rule_name / current_value / threshold / elapsed.
    - `grep -n "Enforcement Events" scripts/validate_controls.py` confirms the dashboard reminder block (CTL-03 confirmation) is present.
    - The script masks the metering key (`api_key[:12]`) and prints no full key value.
    - `ruff check scripts/validate_controls.py` reports no new errors.
  </acceptance_criteria>
  <done>validate_controls.py runs a timed propagate(), reports the halting agent and elapsed time, reminds the operator to confirm the dashboard event, and is keyless-safe (exits 0 without live keys).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BudgetExceededError → CLI render | Exception fields (rule name, cost, rule id) cross into operator-visible Rich output |
| validate_controls.py → Revenium polling API | rev_mk_* metering key authenticates the enforcement poll (read-only scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `_render_budget_halt_panel`, validate_controls.py output | mitigate | Panel/script print only rule_name, cost values, rule_id, and per-agent token counts — never raw key values; validate_controls.py masks the metering key as `api_key[:12]...` (repo convention) |
| T-03-07 | Information Disclosure | BudgetExceededError message surfaced on stage | accept | Rule name and cost figures in the halt panel are the point of the demo — not secrets in this context (RESEARCH §Security Domain) |
| T-03-08 | Repudiation / Confusion | fabricated decision after halt | mitigate | D-05: the halt path prints NO BUY/HOLD/SELL; explicit "no trading decision produced" message so the audience is not misled |
| T-03-SC | Tampering | package installs | accept | No new packages this phase — no supply-chain checkpoint required |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_revenium_enforcement.py -q` green (engine + propagation + panel tests).
- `.venv/bin/python -m pytest -q` (full suite) green with no live keys.
- `grep -n "except BudgetExceededError" cli/main.py` and `grep -n "stop_polling" cli/main.py tradingagents/graph/trading_graph.py` confirm both catch and both teardown paths.
- `python scripts/validate_controls.py` exits 0 in keyless mode.
</verification>

<success_criteria>
- The CLI surfaces a breach as a graceful cost-context halt panel with non-zero exit and no fabricated decision (CTL-02, D-05).
- The enforcement daemon is torn down on both the propagate() and CLI streaming paths.
- validate_controls.py de-risks demo timing (which agent halts, elapsed seconds) and reminds the operator to confirm the dashboard ENFORCEMENT_VIOLATION event (CTL-03, D-09).
</success_criteria>

<output>
Create `.planning/phases/03-cost-controls/03-03-SUMMARY.md` when done.
</output>
