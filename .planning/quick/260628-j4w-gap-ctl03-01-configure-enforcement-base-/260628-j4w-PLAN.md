---
phase: quick
plan: 260628-j4w
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - scripts/validate_controls.py
  - tests/test_revenium_enforcement.py
autonomous: true
requirements: [GAP-CTL03-01]
gap_closure: true

must_haves:
  truths:
    - "Operator can read .env.example and learn that REVENIUM_ENFORCEMENT_BASE_URL must be set to https://api.revenium.ai/profitstream for the in-process enforcement gate to reach the compiled-rules feed."
    - "Operator can read .env.example and learn the Revenium API key (REVENIUM_METERING_API_KEY) must have enforcement-rule READ scope (rev_sk_ works for both metering and enforcement reads; metering-only rev_mk_ is forbidden on the enforcement feed)."
    - "scripts/validate_controls.py docstring Requirements section lists REVENIUM_ENFORCEMENT_BASE_URL, the key-scope requirement, and the ~30s recompile / ~30-60s pre-warm lead-time note."
    - "A keyless unit test proves enforcement._get_enforcement_base_url() returns https://api.revenium.ai/profitstream when REVENIUM_ENFORCEMENT_BASE_URL is set."
  artifacts:
    - path: ".env.example"
      provides: "Documented REVENIUM_ENFORCEMENT_BASE_URL knob + key-scope note near circuit-breaker vars"
      contains: "REVENIUM_ENFORCEMENT_BASE_URL"
    - path: "scripts/validate_controls.py"
      provides: "Updated Requirements docstring (base URL, key scope, recompile cadence)"
      contains: "REVENIUM_ENFORCEMENT_BASE_URL"
    - path: "tests/test_revenium_enforcement.py"
      provides: "Keyless config-resolution test for _get_enforcement_base_url()"
      contains: "_get_enforcement_base_url"
  key_links:
    - from: "tests/test_revenium_enforcement.py"
      to: "revenium_middleware._core.enforcement._get_enforcement_base_url"
      via: "monkeypatch.setenv + direct call"
      pattern: "_get_enforcement_base_url"
---

<objective>
Close GAP-CTL03-01 by CONFIGURATION + DOCS ONLY: make the operator able to arm the
in-process Revenium circuit-breaker so check_enforcement() reaches the correct compiled-rules
feed and fires BudgetExceededError on breach.

The spike (2026-06-28) proved the UNPATCHED vendored SDK works end-to-end when two facts hold:
1. REVENIUM_ENFORCEMENT_BASE_URL=https://api.revenium.ai/profitstream is set (the SDK's
   _get_enforcement_base_url() honors it verbatim; without it the poller derives the bare
   metering origin and 404s).
2. The single Revenium key the SDK uses for BOTH metering and the enforcement-rules fetch
   (REVENIUM_METERING_API_KEY / config revenium_api_key) has enforcement-READ scope — a
   rev_sk_ write key returns 200 and can also meter; a metering-only rev_mk_ key gets 403.

Purpose: Restore the demo's headline graceful-halt moment (CTL-01/CTL-02/D-05) without touching
the vendored SDK and without any monkeypatch/wrapper.
Output: Documented .env.example knob, updated validator docstring, keyless config-resolution test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-cost-controls/03-HUMAN-UAT.md
@.env.example
@scripts/validate_controls.py
@tests/test_revenium_enforcement.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document REVENIUM_ENFORCEMENT_BASE_URL and key-scope requirement in .env.example</name>
  <files>.env.example</files>
  <action>
In the existing Revenium circuit-breaker block (the commented section beginning "# Revenium
cost-control circuit breaker (Phase 3 ...)", around the REVENIUM_CB_*/REVENIUM_BYPASS knobs),
add a new commented subsection for REVENIUM_ENFORCEMENT_BASE_URL. Place it adjacent to the other
circuit-breaker knobs (e.g. just after REVENIUM_CIRCUIT_BREAKER_ENABLED or before
REVENIUM_CB_POLL_INTERVAL_SECONDS) so an operator arming the breaker sees all related vars together.

The new entry must:
- Set the commented default to the proven value: REVENIUM_ENFORCEMENT_BASE_URL=https://api.revenium.ai/profitstream
- Explain in a comment that the in-process enforcement gate fetches compiled rules from this base
  URL; without the /profitstream context path the SDK derives the bare metering origin and the
  feed 404s.
- State the key-scope requirement: the Revenium API key configured as REVENIUM_METERING_API_KEY
  must have enforcement-rule READ scope. A rev_sk_ write key works for BOTH metering and enforcement
  reads; a metering-only rev_mk_ key is forbidden on the enforcement feed (returns 403), so the
  breaker will never arm with a rev_mk_-only key.
- Keep the line commented-out by default (leading '#') so the keyless test suite stays green (DMO-04),
  matching the surrounding REVENIUM_CB_* style.

Do NOT put any real key value anywhere. Match the surrounding comment voice and the existing
'#VAR=value' commented-default pattern used for REVENIUM_METERING_BASE_URL / REVENIUM_PLATFORM_BASE_URL.
  </action>
  <verify>
    <automated>grep -n "REVENIUM_ENFORCEMENT_BASE_URL=https://api.revenium.ai/profitstream" .env.example && grep -A2 -B2 "REVENIUM_ENFORCEMENT_BASE_URL" .env.example | grep -iE "rev_sk_|read scope|enforcement-rule" </automated>
  </verify>
  <done>.env.example contains a commented REVENIUM_ENFORCEMENT_BASE_URL=https://api.revenium.ai/profitstream entry within the circuit-breaker block, with a comment documenting the /profitstream rationale and the enforcement-READ key-scope requirement (rev_sk_ ok for both, rev_mk_ forbidden on the feed). No real key value present.</done>
</task>

<task type="auto">
  <name>Task 2: Update validate_controls.py docstring Requirements section</name>
  <files>scripts/validate_controls.py</files>
  <action>
Edit only the module-level docstring "Requirements:" block (lines ~12-17). Add two requirement
bullets and one note:
- Add a bullet for REVENIUM_ENFORCEMENT_BASE_URL=https://api.revenium.ai/profitstream (so the gate
  reaches the compiled-rules feed).
- Amend/extend the existing REVENIUM_METERING_API_KEY bullet (currently "set to a valid rev_mk_*
  key") to state the key must have enforcement-rule READ scope — a rev_sk_ write key works for both
  metering and enforcement reads; a metering-only rev_mk_ key is rejected (403) by the enforcement
  feed and the breaker will not arm.
- Add a one-line note that the compiled-rules feed recompiles on a ~30s cadence, so allow ~30-60s
  pre-warm lead time after the rule is breached before the gate sees it (D-08 demo lead-time).

Optionally reflect the new env var in the Usage example line so the documented invocation is
correct, but keep edits minimal and confined to the docstring. Do NOT change any executable logic.
Preserve ruff-clean style (no line-length concerns — E501 is ignored).
  </action>
  <verify>
    <automated>python -c "import ast,sys; d=ast.get_docstring(ast.parse(open('scripts/validate_controls.py').read())); sys.exit(0 if ('REVENIUM_ENFORCEMENT_BASE_URL' in d and 'read scope' in d.lower() and ('30s' in d or '~30' in d)) else 1)"</automated>
  </verify>
  <done>The module docstring's Requirements section lists REVENIUM_ENFORCEMENT_BASE_URL, states the enforcement-READ key-scope requirement, and notes the ~30s recompile cadence / ~30-60s pre-warm lead time. No executable code changed; ruff lint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add keyless config-resolution unit test for _get_enforcement_base_url()</name>
  <files>tests/test_revenium_enforcement.py</files>
  <behavior>
    - When REVENIUM_ENFORCEMENT_BASE_URL=https://api.revenium.ai/profitstream is set,
      enforcement._get_enforcement_base_url() returns "https://api.revenium.ai/profitstream"
      (trailing slash stripped if supplied).
    - When REVENIUM_ENFORCEMENT_BASE_URL is unset, _get_enforcement_base_url() falls back to the
      bare metering origin (scheme://netloc, e.g. "https://api.revenium.ai") and does NOT include
      the /profitstream context path — demonstrating why the env var is required (the gap's 404 cause).
  </behavior>
  <action>
Append a new test class (e.g. TestEnforcementBaseUrlResolution) to tests/test_revenium_enforcement.py,
following the file's existing conventions: @pytest.mark.unit on each method, accept the monkeypatch
fixture, use monkeypatch.setenv/delenv (no live credentials, no network — D-10 keyless discipline).
The autouse _reset_enforcement fixture already reloads the enforcement module per test; the new
tests call enforcement._get_enforcement_base_url() directly (it reads os.environ live, no reload
needed).

Test 1 (primary, satisfies constraint (c)): monkeypatch.setenv("REVENIUM_ENFORCEMENT_BASE_URL",
"https://api.revenium.ai/profitstream"); assert enforcement._get_enforcement_base_url() ==
"https://api.revenium.ai/profitstream".

Test 2 (fallback, documents the gap): monkeypatch.delenv("REVENIUM_ENFORCEMENT_BASE_URL",
raising=False); assert the returned value equals the bare metering origin (e.g.
"https://api.revenium.ai") and that "/profitstream" is NOT in the returned value. Set
REVENIUM_METERING_BASE_URL deterministically via monkeypatch.setenv to avoid depending on ambient
env (e.g. set it to "https://api.revenium.ai/meter/" then assert origin "https://api.revenium.ai").

Import enforcement is already at module top (from revenium_middleware._core import enforcement).
Keep imports isort-clean; add no new module-level imports beyond what exists. Add a docstring to
the class/methods consistent with the file's existing docstring style.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_revenium_enforcement.py -k base_url -q</automated>
  </verify>
  <done>New unit tests pass keyless: the set-case asserts "https://api.revenium.ai/profitstream"; the fallback case asserts the bare metering origin with no /profitstream. Full file still passes (.venv/bin/python -m pytest tests/test_revenium_enforcement.py -q) and ruff lint is clean.</done>
</task>

</tasks>

<verification>
- `grep "REVENIUM_ENFORCEMENT_BASE_URL" .env.example scripts/validate_controls.py tests/test_revenium_enforcement.py` shows the knob documented in all three places.
- `.venv/bin/python -m pytest tests/test_revenium_enforcement.py -q` passes with no live credentials.
- `.venv/bin/ruff check .env.example scripts/validate_controls.py tests/test_revenium_enforcement.py` (ruff ignores .env.example; lint clean on the two .py files).
- No real API key value committed in any file (`grep -rE "rev_(mk|sk)_[A-Za-z0-9]{8,}" .env.example scripts/validate_controls.py tests/test_revenium_enforcement.py` returns nothing).
- No change to vendored `revenium_middleware` SDK; no monkeypatch/wrapper added to repo source.
</verification>

<success_criteria>
- GAP-CTL03-01 closed via configuration + docs only: an operator following .env.example and the
  validate_controls.py docstring can arm the in-process enforcement gate so check_enforcement()
  reaches the /profitstream compiled-rules feed with an enforcement-READ-scoped key and halts on breach.
- Keyless test proves _get_enforcement_base_url() resolves to the profitstream context path when the
  env var is set, and falls back to the bare origin (the documented 404 cause) when unset.
- Zero SDK edits, zero monkeypatches, zero committed secrets.
</success_criteria>

<output>
Create `.planning/quick/260628-j4w-gap-ctl03-01-configure-enforcement-base-/260628-j4w-SUMMARY.md` when done.
</output>
