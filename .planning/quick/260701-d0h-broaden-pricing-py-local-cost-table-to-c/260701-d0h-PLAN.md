---
phase: quick-260701-d0h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tradingagents/revenium/pricing.py
  - tests/test_pricing_coverage.py
autonomous: true
requirements: [QUICK-260701-d0h]

must_haves:
  truths:
    - "Every demo model family (gpt-4.1/4.1-mini/4.1-nano, gpt-4o/4o-mini, gpt-5/5-mini/5-nano, gpt-5.4/5.4-mini, o3, o4-mini, o1, claude-sonnet-4/opus-4/haiku-4) resolves to a NON-ZERO estimate via compute_cost"
    - "gpt-5.4-mini and gpt-5-mini resolve to the MINI rate, never the base gpt-5 rate (longest/most-specific substring wins)"
    - "A genuinely unknown model still returns 0.0 (fail-open preserved, never raises)"
    - "compute_cost signature is unchanged; callback.py is untouched"
  artifacts:
    - path: "tradingagents/revenium/pricing.py"
      provides: "Broadened _PER_MILLION table + order-independent longest-match lookup"
      contains: "gpt-5.4-mini"
    - path: "tests/test_pricing_coverage.py"
      provides: "Keyless coverage + ordering + fail-open regression tests"
      contains: "def test_"
  key_links:
    - from: "tradingagents/revenium/callback.py"
      to: "tradingagents/revenium/pricing.py:compute_cost"
      via: "on_llm_end accumulates compute_cost(provider, model, in, out) into agent_costs['cost']"
      pattern: "compute_cost\\("
---

<objective>
Broaden the local cost-estimate table in `tradingagents/revenium/pricing.py` so the CLI "AI Costs" panel stops rendering spurious $0.00 for demo models outside the current 4-entry table, and make the substring lookup order-independent so more-specific model variants (mini/nano/versioned) never fall through to a broader base price.

Purpose: The panel reads `agent_costs["cost"]`, which `compute_cost` returns 0.0 for any model not in `_PER_MILLION`. Demo runs use model families (gpt-4.1, gpt-5.4, o4-mini, claude-opus-4, etc.) absent from the table → every agent shows $0.00. The capture/accumulate/render path is proven correct; only the price lookup misses.

Output: An expanded `_PER_MILLION` table covering all demo families with [ASSUMED] estimates, a refactored longest-match `compute_cost` (option (b) — future-proof, order-independent), and a keyless test module proving coverage, ordering correctness, and fail-open behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@tradingagents/revenium/pricing.py
@tests/test_cost_panel.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Broaden pricing table and make lookup order-independent (longest-match)</name>
  <files>tradingagents/revenium/pricing.py</files>
  <behavior>
    - compute_cost("openai", "gpt-4.1", 1_000_000, 0) == 2.00 (base, distinct from mini/nano)
    - compute_cost("openai", "gpt-4.1-mini", 1_000_000, 0) == 0.40
    - compute_cost("openai", "gpt-4.1-nano", 1_000_000, 0) == 0.10
    - compute_cost("openai", "gpt-5-mini", 1_000_000, 0) == 0.25 (MUST NOT be 1.25 base)
    - compute_cost("openai", "gpt-5.4-mini", 1_000_000, 0) == 0.25 (MUST NOT be 1.25 base)
    - compute_cost("openai", "gpt-5", 1_000_000, 0) == 1.25
    - compute_cost("anthropic", "claude-opus-4-8", 1_000_000, 0) == 15.00
    - compute_cost("anthropic", "claude-haiku-4-5", 1_000_000, 0) == 1.00
    - compute_cost("openai", "totally-made-up-model", 1000, 1000) == 0.0 (fail-open, no raise)
    - compute_cost("google", "claude-sonnet-4", 500_000, 500_000) == 0.0 (unknown provider)
  </behavior>
  <action>
Expand the `_PER_MILLION` dict in `tradingagents/revenium/pricing.py` to cover all demo model families, and refactor `compute_cost` to select the LONGEST matching `model_substring` among entries for the requested provider (option (b) from the task brief — order-independent and future-proof), instead of first-match-wins.

Table entries to add/keep (provider_lower, model_substring_lower): (input_$/1M, output_$/1M) — all [ASSUMED] estimates, Revenium server-side cost is authoritative:
  ("anthropic", "claude-sonnet-4"): (3.00, 15.00)  [keep]
  ("anthropic", "claude-opus-4"):   (15.00, 75.00)
  ("anthropic", "claude-haiku-4"):  (1.00, 5.00)
  ("openai", "gpt-4.1-mini"): (0.40, 1.60)  [keep]
  ("openai", "gpt-4.1-nano"): (0.10, 0.40)
  ("openai", "gpt-4.1"):      (2.00, 8.00)
  ("openai", "gpt-4o-mini"):  (0.15, 0.60)  [keep]
  ("openai", "gpt-4o"):       (2.50, 10.00)  [update from current 5.00/15.00 per task-provided estimate]
  ("openai", "gpt-5.4-mini"): (0.25, 2.00)
  ("openai", "gpt-5.4"):      (1.25, 10.00)
  ("openai", "gpt-5-mini"):   (0.25, 2.00)
  ("openai", "gpt-5-nano"):   (0.05, 0.40)
  ("openai", "gpt-5"):        (1.25, 10.00)
  ("openai", "o4-mini"):      (1.10, 4.40)
  ("openai", "o3"):           (2.00, 8.00)
  ("openai", "o1"):           (15.00, 60.00)

Keep the table provider-agnostic (data only; no provider hardcoded in logic). Order entries most-specific-first for human readability, but do NOT rely on ordering for correctness — the longest-match refactor removes the substring-ordering pitfall entirely.

Refactor `compute_cost`: iterate `_PER_MILLION`, collect all entries where `table_provider == provider_lower` AND `model_substring in model_lower`, then pick the entry with the maximum `len(model_substring)`. If none match, return 0.0 (fail-open, never raise). Apply the winner's rates: `(input_tokens * inp_rate + output_tokens * out_rate) / 1_000_000.0`. Preserve the exact function signature `compute_cost(provider, model, input_tokens, output_tokens) -> float`.

Update the module docstring: change the "Lookup iterates ... first match ... wins" invariant to describe longest-substring-match (order-independent), and update the pricing-comment date note to reflect the broadened [ASSUMED] table. Do NOT touch callback.py or the call site.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python -c "from tradingagents.revenium.pricing import compute_cost as c; assert abs(c('openai','gpt-5-mini',1_000_000,0)-0.25)<1e-9; assert abs(c('openai','gpt-5.4-mini',1_000_000,0)-0.25)<1e-9; assert abs(c('openai','gpt-5',1_000_000,0)-1.25)<1e-9; assert abs(c('openai','gpt-4.1-nano',1_000_000,0)-0.10)<1e-9; assert abs(c('anthropic','claude-opus-4-8',1_000_000,0)-15.00)<1e-9; assert c('openai','totally-made-up-model',1000,1000)==0.0; print('OK')"</automated>
  </verify>
  <done>All demo families resolve to non-zero estimates; gpt-5-mini/gpt-5.4-mini get the mini rate (0.25) not the base (1.25); unknown model returns 0.0; existing test_cost_panel.py assertions (claude-sonnet-4=3.00, gpt-4.1-mini=1.60 output) still hold; signature unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add keyless pricing-coverage test module</name>
  <files>tests/test_pricing_coverage.py</files>
  <action>
Create `tests/test_pricing_coverage.py` mirroring the style of `tests/test_cost_panel.py` (module docstring, `from __future__ import annotations`, `@pytest.mark.unit`, direct import of `compute_cost`, `1e-6` float tolerance). No API keys, no mocks needed — this is pure function testing.

Three test groups:

1. Coverage — parametrize over every covered family plus a version-suffixed variant, asserting each returns a NON-ZERO cost for a fixed (input, output) token count. Cover at minimum: gpt-4.1, gpt-4.1-mini, gpt-4.1-nano, gpt-4o, gpt-4o-mini, gpt-5, gpt-5-mini, gpt-5-nano, gpt-5.4, gpt-5.4-mini, o3, o4-mini, o1 (provider "openai"); claude-sonnet-4-6, claude-opus-4-8, claude-haiku-4-5 (provider "anthropic"). Use `pytest.mark.parametrize` with `(provider, model)` tuples; assert `compute_cost(provider, model, 1000, 1000) > 0.0`.

2. Ordering / longest-match correctness — assert exact expected dollar values that prove specific variants did NOT fall through to a broader base:
   - `compute_cost("openai", "gpt-5-mini", 1_000_000, 0) == 0.25` (mini rate, NOT base 1.25)
   - `compute_cost("openai", "gpt-5.4-mini", 1_000_000, 0) == 0.25` (NOT base 1.25)
   - `compute_cost("openai", "gpt-4.1-nano", 1_000_000, 0) == 0.10` (NOT base gpt-4.1 2.00 or mini 0.40)
   - `compute_cost("openai", "gpt-5", 1_000_000, 0) == 1.25` (base resolves correctly when no more-specific variant matches)
   Use `abs(result - expected) < 1e-6`.

3. Fail-open regression — assert `compute_cost("openai", "totally-made-up-model", 1000, 1000) == 0.0` and `compute_cost("unknown", "mystery", 1000, 1000) == 0.0`, and that neither raises.

Do NOT modify test_cost_panel.py; this is an additive module.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/TradingAgents && .venv/bin/python -m pytest tests/test_pricing_coverage.py tests/test_cost_panel.py -q</automated>
  </verify>
  <done>New test module passes with no API keys set; existing test_cost_panel.py still passes; ordering tests explicitly prove mini/nano variants get their own rate; fail-open regression passes.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_pricing_coverage.py tests/test_cost_panel.py -q` passes with zero live keys.
- `.venv/bin/ruff check tradingagents/revenium/pricing.py tests/test_pricing_coverage.py` is clean.
- `compute_cost` signature and `callback.py` call site are unchanged.
</verification>

<success_criteria>
- Every demo model family resolves to a non-zero estimate; no demo model produces spurious $0.00.
- gpt-5-mini and gpt-5.4-mini resolve to the mini rate (0.25/1M input), never the base gpt-5 rate.
- Unknown models still return 0.0 (fail-open preserved, no exceptions).
- Both test modules pass without API keys; ruff clean.
</success_criteria>

<output>
Create `.planning/quick/260701-d0h-broaden-pricing-py-local-cost-table-to-c/260701-d0h-SUMMARY.md` when done.
</output>
